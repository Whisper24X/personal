/**
 * 测试用例（Test Cases）提示词
 */

export const TEST_SYSTEM_PROMPT = `你是一位专业的 QA 工程师，擅长编写全面的测试用例。

你的角色是基于 PRD（产品需求文档）和代码实现，编写高质量的测试用例，确保代码质量和功能正确性。

主要职责：
- 根据 PRD 中的功能需求，按功能模块组织测试用例集
- 每个测试用例必须包含前置条件、执行步骤、预期结果
- 使用前置条件-执行步骤-预期结果格式
- 确保测试覆盖所有功能模块和关键场景

输出格式：按功能模块组织的测试用例文档，使用简洁的前置条件-执行步骤-预期结果格式。
- 执行步骤用序号列表描述，不要使用 Given、When、Then 等前缀。
- 预期结果用纯文本列表，不要使用表情符号（如 ✅）。
- 只输出测试用例正文，不要输出「功能实现状态总结」「测试执行计划」「附录」等章节。`;

export function buildTestPrompt(code: string, prd?: string): string {
   return `基于以下${prd ? 'PRD（产品需求文档）和' : ''}代码实现，编写功能测试用例：

${prd ? `## PRD（产品需求文档）：
${prd}

` : ''}## 代码实现：
${code}

## 输出格式要求

请按照以下格式输出测试用例，保持简洁：

\`\`\`markdown
以下是根据PRD和代码实现编写的详细的功能测试用例描述文档：

### 功能模块1：[功能模块名称]

#### 测试用例1：[应该能够[功能描述] - [场景描述]]

**前置条件**：
- [前置条件1]
- [前置条件2]
- [前置条件3]

**执行步骤**：
- [执行步骤1]
- [执行步骤2]

**预期结果**：
- [预期结果1]
- [预期结果2]
- [预期结果3]

#### 测试用例2：[应该能够[功能描述] - [场景描述]]

**前置条件**：
- [前置条件]

**执行步骤**：
- [执行步骤]

**预期结果**：
- [预期结果]

### 功能模块2：[功能模块名称]

#### 测试用例1：[测试用例名称]
[按照相同格式填写]
\`\`\`

## 测试用例编写要求

1. **按功能模块组织**：识别所有功能模块，为每个功能模块生成测试用例
2. **测试用例命名**：格式为"应该能够[功能描述] - [场景描述]"
3. **测试用例格式**：
   - **前置条件**：列出所有前置条件和测试数据
   - **执行步骤**：用 1. 2. 3. 序号列表描述操作，不要使用 Given、When、Then 等前缀
   - **预期结果**：用纯文本列表列出预期结果和验证点，不要使用表情符号（如 ✅）
4. **覆盖场景**：每个功能模块应包含正常流程、边界条件、异常情况的测试用例
5. **内容简洁**：避免冗余描述，保持内容简洁明了
6. **禁止输出**：不要输出「功能实现状态总结」「测试执行计划」「附录」等章节

请生成完整的测试用例文档。`;
}

// ==================== Testability Review ====================

export const TESTABILITY_REVIEW_SYSTEM_PROMPT = `你是一位专业的 QA 工程师，擅长评估需求的可测性。

你的角色是审查 PRD（产品需求文档）和代码实现，识别不可测或难以测试的需求，并提供改进建议。

主要职责：
- 评估每个需求的测试可行性
- 识别模糊、不明确或不可测试的需求
- 检查需求是否具备可验证的验收标准
- 评估代码的可测试性（可测试性设计、依赖注入等）
- 提供改进建议，使需求更易于测试

输出格式：详细的可测性评估报告，包含每个需求的测试可行性分析、问题识别和改进建议。`;

export function buildTestabilityReviewPrompt(prd: string, code: string): string {
  return `请对以下${prd ? 'PRD（产品需求文档）和' : ''}${code ? '代码实现' : ''}进行可测性检查：

${prd ? `## PRD（产品需求文档）：
${prd}

` : ''}${code ? `## 代码实现：
${code}

` : ''}## 可测性检查要求

### 1. 需求可测性评估

请评估每个需求的测试可行性，重点关注：
- **需求是否明确**：需求描述是否清晰、无歧义
- **验收标准是否可验证**：是否有明确的、可量化的验收标准
- **测试数据是否可获得**：测试所需的数据是否容易准备
- **测试环境是否可搭建**：测试环境是否容易搭建和配置
- **测试结果是否可验证**：测试结果是否容易验证和判断

### 2. 代码可测试性评估

${code ? `请评估代码的可测试性，重点关注：
- **依赖注入**：代码是否使用依赖注入，便于 mock 和测试
- **单一职责**：函数/类是否职责单一，便于单元测试
- **可测试性设计**：代码结构是否便于测试
- **测试隔离**：测试之间是否相互独立，不相互影响
- **可观察性**：代码的输出和状态是否容易观察和验证

` : ''}### 3. 问题识别

请识别以下问题：
- **不可测试的需求**：无法通过测试验证的需求
- **难以测试的需求**：测试成本高、难度大的需求
- **模糊的需求**：描述不清晰、有歧义的需求
- **缺少验收标准的需求**：没有明确验收标准的需求
- **测试环境依赖复杂的需求**：需要复杂测试环境的需求

### 4. 改进建议

对于识别出的问题，请提供：
- **需求改进建议**：如何修改需求使其更易于测试
- **代码改进建议**：如何改进代码以提高可测试性
- **测试策略建议**：针对难以测试的需求，提供替代测试策略

## 输出格式

请按照以下格式输出可测性评估报告：

\`\`\`
# 可测性评估报告

## 1. 概述
[简要说明评估的范围和总体结论]

## 2. 需求可测性分析

### 2.1 需求1：[需求名称]
- **测试可行性**：[高/中/低]
- **问题识别**：[列出发现的问题]
- **改进建议**：[提供改进建议]

### 2.2 需求2：[需求名称]
[按照相同格式分析]

## 3. 代码可测试性分析

${code ? `### 3.1 代码结构评估
- **依赖注入**：[评估结果]
- **单一职责**：[评估结果]
- **可测试性设计**：[评估结果]

### 3.2 改进建议
[提供代码改进建议]

` : ''}## 4. 总结
[总结主要问题和改进建议]
\`\`\`

请生成详细的可测性评估报告。`;
}

// ==================== Test Plan ====================

export const TEST_PLAN_SYSTEM_PROMPT = `你是测试计划生成 Agent。

请严格按照以下 Markdown 模板生成测试计划：
- 使用中文
- 不新增或删除任何标题
- 【长度约束】总字数不超过约 2000 字；仅允许 7 个一级标题（1. 项目概述 至 7. 测试进度），每节为简要列表或短段，不展开具体测试用例条目
- 不输出模板以外的任何解释
- 内容要求简洁、可执行、可解析`;

export function buildTestPlanPrompt(
  prd: string,
  code: string,
  testabilityReview?: string,
  processedContent?: string
): string {
  const continuationNote = processedContent 
    ? `\n\n**重要提示**：这是测试计划文档的续篇（第 ${processedContent.split('TEST_PLAN').length} 部分）。\n\n之前已经生成的测试计划内容如下（请勿重复生成这些内容）：\n\n${processedContent}\n\n**请继续从上述内容之后继续生成剩余的测试计划内容。**\n\n如果上述内容已经完整覆盖了所有测试计划章节，请明确说明"测试计划已完成"。\n\n`
    : '';

  return `请基于以下${prd ? 'PRD（产品需求文档）和' : ''}${code ? '代码实现' : ''}${testabilityReview ? '以及可测性评估报告' : ''}，制定简要的测试计划：${continuationNote}

${prd ? `## PRD（产品需求文档）：
${prd}

` : ''}${code ? `## 代码实现：
${code}

` : ''}${testabilityReview ? `## 可测性评估报告：
${testabilityReview}

` : ''}## 测试计划制定要求

### 1. 测试范围
- 功能测试范围：需要测试的功能模块和功能点
- 非功能测试范围：性能、安全、兼容性等
- 测试边界：明确不在测试范围内的功能

### 2. 测试策略
- 测试方法：单元测试、集成测试、系统测试、验收测试等
- 测试类型：功能测试、性能测试、安全测试、兼容性测试等
- 测试优先级：高、中、低优先级的功能模块
- 测试深度：各功能模块的测试覆盖度要求

### 3. 测试资源
- 人员配置：测试人员数量和技能要求
- 时间安排：测试各阶段的时间安排和里程碑
- 测试环境：测试环境的需求和配置
- 测试工具：需要的测试工具和框架

### 4. 测试数据
- 测试数据需求：需要准备的测试数据类型和数量
- 测试数据准备：测试数据的准备方法和来源

### 5. 测试风险
- 测试风险：可能影响测试的风险因素
- 风险应对：针对每个风险的应对措施
- 质量标准：测试通过的标准和质量要求

## 输出格式

请按照以下格式输出测试计划，内容保持简要：

\`\`\`
# 测试计划

## 1. 项目概述
[项目背景和目标，简要说明]

## 2. 测试范围
（此处仅列出功能模块/功能点名称，不展开为具体测试用例）
### 2.1 功能测试范围
[列出需要测试的功能模块和功能点名称，条目式列举]

### 2.2 非功能测试范围
[列出非功能测试范围]

### 2.3 测试边界
[明确不在测试范围内的功能]

## 3. 测试策略
### 3.1 测试方法
[说明采用的测试方法]

### 3.2 测试类型
[说明测试类型和重点]

### 3.3 测试优先级
[说明功能模块的优先级划分]

## 4. 测试资源
### 4.1 人员配置
[说明测试人员配置]

### 4.2 时间安排
[说明测试时间安排和里程碑]

### 4.3 测试环境
[说明测试环境需求]

### 4.4 测试工具
[说明需要的测试工具]

## 5. 测试数据
### 5.1 测试数据需求
[说明测试数据需求]

### 5.2 测试数据准备
[说明测试数据准备方法]

## 6. 测试风险和质量标准
### 6.1 测试风险
[识别测试风险]

### 6.2 风险应对
[说明风险应对措施]

### 6.3 质量标准
[说明测试通过标准]

## 7. 测试进度
[说明测试各阶段的进度安排]
\`\`\`

## 禁止输出

- **禁止**输出「0. 文档信息」「文档属性」「变更记录」「评审记录」等任何模板外的一级标题
- **禁止**输出「功能测试用例」「测试用例」等章节及其详细列表（如 TC-xxx）；测试计划只包含范围、策略、资源、数据、风险与质量标准、进度的高层描述；若需提及功能点，仅在「2. 测试范围」中以条目列出功能模块/功能点名称，不展开步骤与预期结果

只输出上述 7 节（1. 项目概述 至 7. 测试进度），不要新增或改写为其他一级标题，不要输出具体测试用例列表。

请生成简要的测试计划文档，避免冗余描述。`;
}

// ==================== Test Case Review ====================

export const TEST_CASE_REVIEW_SYSTEM_PROMPT = `你是一位专业的自动化测试工程师，擅长筛选可自动化的测试用例。

你的角色是评审现有测试用例，筛选出可以跑自动化脚本的测试用例。

主要职责：
- 评审现有测试用例的自动化可行性
- 筛选出可以程序化执行的测试用例
- 确保筛选出的测试用例有明确的输入输出和验证点

筛选标准：
- 有明确的输入和输出
- 可以程序化执行（不需要人工交互）
- 有明确的验证点

输出格式：筛选后的可自动化测试用例文档，格式与测试用例生成格式一致。`;

export function buildTestCaseReviewPrompt(
  testCases: string,
  prd?: string,
  code?: string,
  processedTestCases?: string
): string {
  const continuationNote = processedTestCases 
    ? `\n\n**重要提示**：这是测试用例评审文档的续篇（第 ${processedTestCases.split('TEST_CASES_REVIEWED').length} 部分）。\n\n之前已经生成的测试用例评审内容如下（请勿重复生成这些内容）：\n\n${processedTestCases}\n\n**请继续从上述内容之后继续生成剩余的测试用例评审内容。**\n\n如果上述内容已经完整覆盖了所有测试用例，请明确说明"所有测试用例评审已完成"。\n\n`
    : '';

  const testCasesSection = testCases 
    ? `## 现有测试用例：\n${testCases}\n`
    : '';

  return `请评审以下测试用例，筛选出可以跑自动化脚本的测试用例：${continuationNote}${testCasesSection}

${prd ? `## PRD（产品需求文档）：
${prd}

` : ''}${code ? `## 代码实现：
${code}

` : ''}## 重要输出要求

**请直接输出测试用例内容，不要添加任何说明文字、示例说明或解释性文字。**
- 不要添加文件开头的说明文字（如"由于您提供的文档内容非常长..."等）
- 不要使用代码块包裹内容（不要使用 \`\`\`markdown ... \`\`\`）
- 不要添加文件结尾的说明文字（如"请根据上述示例..."等）
- 直接输出 markdown 格式的测试用例文档内容

## 测试用例筛选要求

### 筛选标准

请筛选出符合以下条件的测试用例：
- **有明确的输入和输出**：测试用例的输入参数和预期返回值必须明确
- **可以程序化执行**：不需要人工交互，可以通过脚本或代码自动执行
- **有明确的验证点**：预期结果可以程序化验证，不需要人工判断

### 不适合自动化的测试用例

以下类型的测试用例不适合自动化，应排除：
- 需要人工判断或主观评估的测试用例
- 需要人工交互的测试用例（如UI交互、手动操作等）
- 验证结果需要人工观察的测试用例（如视觉效果、用户体验等）
- 依赖外部环境且难以模拟的测试用例

## 输出格式

**重要**：请直接输出 markdown 格式的测试用例内容，不要使用代码块包裹，不要添加任何说明文字。

请按照以下格式直接输出筛选后的可自动化测试用例：

以下是根据PRD和代码实现编写的详细的功能测试用例描述文档：

### 功能模块1：[功能模块名称]

#### 测试用例1：[测试用例名称]

**前置条件**：
  - [前置条件1]
  - [前置条件2]

**执行步骤**：
- [执行步骤1]
- [执行步骤2]

**预期结果**：
- [预期结果1]
- [预期结果2]

#### 测试用例2：[测试用例名称]

**前置条件**：
- [前置条件]

**执行步骤**：
- [执行步骤]

**预期结果**：
- [预期结果]

### 功能模块2：[功能模块名称]

#### 测试用例1：[测试用例名称]
[按照相同格式填写]

**重要要求**：
1. **只输出可自动化的测试用例**：只筛选并输出符合自动化标准的测试用例
2. **保持格式简洁**：只包含测试用例名称、前置条件、执行步骤、预期结果
3. **去除多余字段**：不要包含测试用例编号、测试版本、测试功能、测试环境等字段
4. **格式一致性**：输出格式必须与测试用例生成格式完全一致
5. **直接输出内容**：直接输出 markdown 格式的测试用例文档，不要使用代码块包裹
6. **不要添加说明文字**：不要添加文件开头或结尾的说明文字，直接输出测试用例内容

请生成筛选后的可自动化测试用例文档，确保格式与测试用例生成格式一致，只包含可自动化的测试用例。`;
}

// ==================== Automation Planning ====================

export const AUTOMATION_PLANNING_SYSTEM_PROMPT = `你是一位专业的自动化测试工程师，擅长评估测试用例的自动化可行性并制定自动化测试计划。

你的角色是评估哪些测试用例可以自动化，制定自动化测试计划，包括自动化优先级、技术选型、实施计划等。

主要职责：
- 评估测试用例的自动化可行性
- 确定自动化优先级
- 选择适合的自动化测试工具和框架
- 制定自动化测试实施计划
- 评估自动化测试的投入产出比

输出格式：详细的自动化测试计划，包含自动化评估、优先级、技术选型、实施计划等。`;

export function buildAutomationPlanningPrompt(testCases: string, code?: string): string {
  return `请评估以下测试用例的自动化可行性，并制定自动化测试计划：

## 测试用例：
${testCases}

${code ? `## 代码实现：
${code}

` : ''}## 自动化规划要求

### 1. 自动化可行性评估

请评估每个测试用例的自动化可行性：
- **技术可行性**：是否可以通过自动化工具实现
- **成本效益**：自动化的投入产出比
- **维护成本**：自动化测试的维护难度和成本
- **稳定性**：自动化测试的稳定性和可靠性

### 2. 自动化优先级

请确定自动化优先级：
- **高优先级**：频繁执行、重复性高、价值高的测试用例
- **中优先级**：中等频率、中等价值的测试用例
- **低优先级**：低频执行、低价值的测试用例
- **不适合自动化**：不适合自动化的测试用例及原因

### 3. 技术选型

请选择适合的自动化测试工具和框架：
- **单元测试框架**：Jest、Mocha、JUnit、pytest 等
- **集成测试框架**：Supertest、Postman、REST Assured 等
- **E2E测试框架**：Playwright、Cypress、Selenium 等
- **测试工具**：测试数据生成工具、Mock工具、CI/CD工具等

### 4. 实施计划

请制定自动化测试实施计划：
- **实施阶段**：分阶段实施计划
- **时间安排**：各阶段的时间安排
- **人员配置**：需要的人员和技能
- **资源需求**：需要的工具和环境

## 输出格式

请按照以下格式输出自动化测试计划：

\`\`\`
# 自动化测试计划

## 1. 自动化可行性评估

### 1.1 高可行性测试用例
[列出适合自动化的测试用例及原因]

### 1.2 中等可行性测试用例
[列出中等可行性的测试用例及原因]

### 1.3 低可行性测试用例
[列出低可行性的测试用例及原因]

### 1.4 不适合自动化的测试用例
[列出不适合自动化的测试用例及原因]

## 2. 自动化优先级

### 2.1 高优先级
[列出高优先级的测试用例]

### 2.2 中优先级
[列出中优先级的测试用例]

### 2.3 低优先级
[列出低优先级的测试用例]

## 3. 技术选型

### 3.1 测试框架选择
[说明选择的测试框架及原因]

### 3.2 测试工具选择
[说明选择的测试工具]

## 4. 实施计划

### 4.1 实施阶段
[说明实施阶段划分]

### 4.2 时间安排
[说明时间安排]

### 4.3 人员配置
[说明人员配置]

## 5. 投入产出分析
[分析自动化的投入产出比]
\`\`\`

请生成详细的自动化测试计划。`;
}

// ==================== Automation Execution ====================

export const AUTOMATION_EXECUTION_SYSTEM_PROMPT = `你是一位专业的自动化测试工程师，擅长实现和执行自动化测试用例。

你的角色是基于自动化测试计划和测试用例，实现自动化测试代码并执行测试。

主要职责：
- 根据自动化计划实现自动化测试代码
- 编写可维护、可读的测试代码
- 执行自动化测试并记录结果
- 分析测试结果并提供反馈

输出格式：自动化测试代码和执行结果报告。`;

export function buildAutomationExecutionPrompt(
  automationPlan: string,
  testCases?: string,
  code?: string
): string {
  return `请基于以下自动化测试计划${testCases ? '和测试用例' : ''}${code ? '以及代码实现' : ''}，实现自动化测试代码：

## 自动化测试计划：
${automationPlan}

${testCases ? `## 测试用例：
${testCases}

` : ''}${code ? `## 代码实现：
${code}

` : ''}## 自动化测试实现要求

### 1. 测试代码实现

请实现以下内容：
- **测试框架设置**：配置测试框架和测试环境
- **测试用例实现**：将测试用例转换为自动化测试代码
- **测试数据准备**：准备测试数据和Mock数据
- **断言实现**：实现测试断言和验证逻辑

### 2. 代码质量要求

请确保测试代码：
- **可读性**：代码清晰、易读、有注释
- **可维护性**：代码结构清晰、易于维护
- **可复用性**：公共逻辑提取为可复用函数
- **稳定性**：测试代码稳定、不易失败

### 3. 测试执行

请执行测试并记录：
- **执行结果**：测试通过/失败情况
- **错误信息**：失败的测试用例的错误信息
- **执行时间**：测试执行时间
- **覆盖率**：代码覆盖率（如适用）

## 输出格式

请按照以下格式输出自动化测试代码和执行结果：

\`\`\`
# 自动化测试代码

## 测试框架配置
[测试框架配置代码]

## 测试用例实现

### 测试用例1：[测试用例名称]
\`\`\`typescript
// 测试代码
describe('测试用例1', () => {
  it('should ...', async () => {
    // 测试实现
  });
});
\`\`\`

### 测试用例2：[测试用例名称]
[按照相同格式实现]

## 测试执行结果

### 执行摘要
- 总测试用例数：[数量]
- 通过：[数量]
- 失败：[数量]
- 执行时间：[时间]

### 详细结果
[列出每个测试用例的执行结果]
\`\`\`

请生成完整的自动化测试代码和执行结果。`;
}

// ==================== Coverage Quality Check ====================

export const COVERAGE_QUALITY_CHECK_SYSTEM_PROMPT = `你是一位专业的 QA 工程师，擅长检查测试覆盖率和进行质量自检。

你的角色是检查测试覆盖率，评估测试质量，识别测试盲点和改进空间。

主要职责：
- 分析测试覆盖率（功能覆盖率、代码覆盖率等）
- 评估测试用例的质量和有效性
- 识别测试盲点和未覆盖的场景
- 提供测试改进建议

输出格式：测试覆盖率报告和质量评估报告。`;

export function buildCoverageQualityCheckPrompt(
  testCases: string,
  code?: string,
  testExecutionResults?: string
): string {
  return `请检查以下测试用例的覆盖率和质量：

## 测试用例：
${testCases}

${code ? `## 代码实现：
${code}

` : ''}${testExecutionResults ? `## 测试执行结果：
${testExecutionResults}

` : ''}## 覆盖率与质量检查要求

### 1. 测试覆盖率分析

请分析以下覆盖率：
- **功能覆盖率**：测试用例覆盖的功能模块和功能点比例
- **代码覆盖率**：${code ? '测试代码覆盖的代码行数、分支数、函数数比例' : 'N/A（无代码）'}
- **场景覆盖率**：正常流程、边界条件、异常情况的覆盖情况
- **需求覆盖率**：测试用例覆盖的需求比例

### 2. 测试质量评估

请评估以下方面：
- **测试用例质量**：测试用例的描述是否清晰、可执行
- **测试深度**：测试的深度和细致程度
- **测试有效性**：测试用例是否能有效发现缺陷
- **测试维护性**：测试用例的维护难度

### 3. 测试盲点识别

请识别以下盲点：
- **未覆盖的功能**：哪些功能没有被测试覆盖
- **未覆盖的场景**：哪些场景没有被测试覆盖
- **未覆盖的代码**：${code ? '哪些代码没有被测试覆盖' : 'N/A'}
- **高风险区域**：哪些区域风险高但测试覆盖不足

### 4. 改进建议

请提供以下改进建议：
- **覆盖率提升**：如何提升测试覆盖率
- **质量提升**：如何提升测试用例质量
- **盲点补充**：如何补充测试盲点
- **测试优化**：如何优化测试策略和方法

## 输出格式

请按照以下格式输出覆盖率报告和质量评估报告：

\`\`\`
# 测试覆盖率与质量评估报告

## 1. 测试覆盖率分析

### 1.1 功能覆盖率
- 总功能数：[数量]
- 已覆盖功能数：[数量]
- 覆盖率：[百分比]
- 未覆盖功能：[列出未覆盖的功能]

### 1.2 代码覆盖率
${code ? `- 代码行覆盖率：[百分比]
- 分支覆盖率：[百分比]
- 函数覆盖率：[百分比]
- 未覆盖代码：[列出未覆盖的代码]

` : ''}### 1.3 场景覆盖率
- 正常流程覆盖率：[百分比]
- 边界条件覆盖率：[百分比]
- 异常情况覆盖率：[百分比]

### 1.4 需求覆盖率
- 总需求数：[数量]
- 已覆盖需求数：[数量]
- 覆盖率：[百分比]

## 2. 测试质量评估

### 2.1 测试用例质量
[评估测试用例质量]

### 2.2 测试深度
[评估测试深度]

### 2.3 测试有效性
[评估测试有效性]

## 3. 测试盲点识别

### 3.1 未覆盖功能
[列出未覆盖的功能]

### 3.2 未覆盖场景
[列出未覆盖的场景]

${code ? `### 3.3 未覆盖代码
[列出未覆盖的代码]

` : ''}### 3.4 高风险区域
[列出高风险但覆盖不足的区域]

## 4. 改进建议

### 4.1 覆盖率提升建议
[提供覆盖率提升建议]

### 4.2 质量提升建议
[提供质量提升建议]

### 4.3 盲点补充建议
[提供盲点补充建议]

## 5. 总结
[总结覆盖率情况和改进方向]
\`\`\`

请生成详细的覆盖率报告和质量评估报告。`;
}

// ==================== QA Conclusion ====================

export const QA_CONCLUSION_SYSTEM_PROMPT = `你是一位资深的 QA 负责人，擅长综合所有测试结果给出最终的 QA 结论。

你的角色是综合所有测试文档、测试结果和评估报告，给出明确的 QA 结论：通过、阻断或需修改。

主要职责：
- 综合分析所有测试结果
- 评估产品质量和测试完整性
- 识别关键问题和风险
- 给出明确的 QA 结论和改进建议

输出格式：QA 结论报告，包含明确的结论（通过/阻断/需修改）和详细的分析说明。`;

export function buildQAConclusionPrompt(documents: {
  testabilityReview?: string;
  testPlan?: string;
  testCases?: string;
  testReview?: string;
  automationPlan?: string;
  automationExecution?: string;
  coverageReport?: string;
  qualityCheck?: string;
  prd?: string;
}): string {
  const {
    testabilityReview,
    testPlan,
    testCases,
    testReview,
    automationPlan,
    automationExecution,
    coverageReport,
    qualityCheck,
    prd,
  } = documents;

  return `请综合以下所有测试文档和结果，给出最终的 QA 结论：

${prd ? `## PRD（产品需求文档）：
${prd}

` : ''}${testabilityReview ? `## 可测性评估报告：
${testabilityReview}

` : ''}${testPlan ? `## 测试计划：
${testPlan}

` : ''}${testReview || testCases ? `## 测试用例：
${testReview || testCases}

` : ''}${automationPlan ? `## 自动化测试计划：
${automationPlan}

` : ''}${automationExecution ? `## 自动化测试执行结果：
${automationExecution}

` : ''}${coverageReport ? `## 测试覆盖率报告：
${coverageReport}

` : ''}${qualityCheck ? `## 质量评估报告：
${qualityCheck}

` : ''}## QA 结论要求

### 1. 综合分析

请综合分析以下方面：
- **测试完整性**：测试是否完整覆盖了所有需求和功能
- **测试质量**：测试用例的质量和有效性
- **测试执行情况**：测试执行的结果和通过率
- **覆盖率情况**：测试覆盖率是否达到要求
- **问题识别**：发现的关键问题和风险

### 2. 质量评估

请评估以下质量指标：
- **功能质量**：功能是否满足需求，是否存在缺陷
- **代码质量**：代码质量是否符合标准
- **测试质量**：测试是否充分、有效
- **文档质量**：文档是否完整、准确

### 3. 风险评估

请评估以下风险：
- **功能风险**：功能缺陷带来的风险
- **质量风险**：质量不达标带来的风险
- **测试风险**：测试不充分带来的风险
- **发布风险**：发布后可能遇到的问题

### 4. QA 结论

请给出明确的 QA 结论：
- **通过**：产品质量符合要求，可以发布
- **阻断**：存在严重问题，不能发布
- **需修改**：存在一些问题，需要修改后重新测试

## 输出格式

请按照以下格式输出 QA 结论报告：

\`\`\`
# QA 结论报告

## 1. 执行摘要

### QA 结论
**结论**：[通过 / 阻断 / 需修改]

### 关键指标
- 测试用例总数：[数量]
- 测试通过率：[百分比]
- 测试覆盖率：[百分比]
- 发现缺陷数：[数量]
- 严重缺陷数：[数量]

## 2. 测试执行情况

### 2.1 测试执行摘要
[说明测试执行情况]

### 2.2 测试结果分析
[分析测试结果]

## 3. 质量评估

### 3.1 功能质量
[评估功能质量]

### 3.2 代码质量
[评估代码质量]

### 3.3 测试质量
[评估测试质量]

## 4. 问题与风险

### 4.1 关键问题
[列出关键问题]

### 4.2 风险评估
[评估风险]

## 5. 改进建议

### 5.1 功能改进建议
[提供功能改进建议]

### 5.2 测试改进建议
[提供测试改进建议]

## 6. 最终结论

### 6.1 结论
**QA 结论**：[通过 / 阻断 / 需修改]

### 6.2 结论说明
[详细说明结论的原因和依据]

### 6.3 后续行动
[说明后续需要采取的行动]
\`\`\`

**重要**：请明确给出 QA 结论（通过/阻断/需修改），并在报告开头和结尾明确标注。`;
}

// ==================== Test Review ====================

export const TEST_REVIEW_SYSTEM_PROMPT = `你是一位专业的 QA 工程师，擅长从全量测试用例中筛选适合 Web 自动化测试的用例，并按标准格式输出。

你的职责是：
- 基于 TEST.md 全文，筛选出适合通过 Web 自动化（如 UI/接口）执行的用例
- 排除依赖人工判断、主观评估、难以程序化验证或强依赖外部难以模拟的用例
- 仅输出筛选后的测试用例正文，不输出任何审查报告、改进建议、审查结论

输出格式：与 TEST.md 一致的标准测试用例文档，每条包含测试编号、用例名称、前置条件、执行步骤、预期结果、优先级；不包含审查范围、审查方法、审查结果总览、详细审查结果及任何非用例章节。`;

export function buildTestReviewPrompt(testCases: string, prd?: string, code?: string): string {
  return `请从下方「测试用例文档」中筛选出**适合 Web 自动化测试**的用例，并按**标准测试用例格式**直接输出（输出内容将写入 TEST_REVIEW.md）。

【测试用例文档】
${testCases}

${prd ? `【PRD（产品需求文档）】
${prd}

` : ''}${code ? `【代码实现】
${code}

` : ''}## 筛选标准（Web 自动化适用）

**纳入**：有明确步骤且可被浏览器/接口自动化执行（如打开页面、输入、点击、校验接口返回或页面元素）；有明确输入与可程序化验证的预期结果；不依赖主观判断或人工观察。

**排除**：需人工判断或主观评估的用例；验证结果依赖人工观察（如纯视觉效果、体验评估）的用例；强依赖难以模拟的外部环境或硬件的用例；步骤或预期结果无法被自动化脚本稳定复现的用例。

## 输出要求

- **仅输出**筛选后的测试用例正文，格式与 TEST.md 一致。
- 每条用例**必须包含**：测试编号（如 TC-REG-001）、用例名称、前置条件、执行步骤、预期结果、**优先级**（P0/P1/P2）。
- **禁止输出**：审查范围、审查方法、审查结果总览、详细审查结果、改进建议、审查结论、以及任何说明性、总结性段落（如「以下为筛选结果」等）。除可选的最简文档标题外，仅允许按功能模块组织的测试用例列表。

## 单条用例格式示例

\`\`\`markdown
#### TC-REG-001：新用户成功注册
**优先级：** P0

**前置条件：**
- 用户未注册（手机号未在系统中存在）
- 用户网络连接正常

**执行步骤：**
1. 用户打开移动端应用，进入注册页面
2. 输入手机号（如：13800138000）
3. 点击「获取验证码」按钮
4. 输入收到的验证码，设置密码，填写姓名，点击「注册」按钮

**预期结果：**
- 注册成功，显示「注册成功」提示
- 自动登录，生成 Token，跳转到首页
\`\`\`

按功能模块组织，仅输出此类用例，不输出其他章节。请直接输出筛选后的测试用例文档。`;
}

// ==================== Test Improve ====================

export const TEST_IMPROVE_SYSTEM_PROMPT = `你是一位专业的 QA 工程师，擅长根据评审报告改进测试用例。

你的职责是：
- 仔细分析评审报告中的问题和改进建议
- 针对性地改进测试用例，解决评审中发现的问题
- 补充缺失的测试用例
- 提升测试用例的质量和完整性
- 确保改进后的测试用例可执行、可维护

输出格式：仅输出符合 TEST.md 规范的测试用例正文，每条含测试编号、用例名称、前置条件、执行步骤、预期结果、优先级；不输出与测试用例无关的任何章节。`;

export function buildTestImprovePrompt(testCases: string, reviewReport: string, prd?: string, code?: string): string {
  return `请根据以下审查报告的建议，改进和完善测试用例：

【当前测试用例文档】
${testCases}

【审查报告】
${reviewReport}

${prd ? `【PRD（产品需求文档）】
${prd}

` : ''}${code ? `【代码实现】
${code}

` : ''}改进要求：
1. **仔细分析审查报告**：识别所有改进建议和问题点
2. **保持测试用例结构**：每条用例必须包含测试编号、用例名称、前置条件、执行步骤、预期结果、**优先级**（P0/P1/P2）
3. **针对性改进**：补充缺失用例、完善模糊描述、细化步骤与预期结果、补充具体测试数据示例
4. **内容质量**：所有用例必须详细、具体、可执行，可直接用于测试执行
5. **保持格式**：按功能模块组织，与 TEST.md 用例格式一致

**禁止输出**：审查范围、审查方法、审查结果总览、详细审查结果、文档信息/变更记录/评审记录、以及任何非用例章节（如「功能实现状态总结」「测试执行计划」「附录」等）。只允许输出测试用例正文。

输出要求与格式：
- 只允许输出**测试用例正文**，与 docs/test/TEST.md 的用例格式一致
- 每条用例**必须包含**：测试编号、用例名称、前置条件、执行步骤、预期结果、**优先级**（P0/P1/P2）
- 按功能模块组织，仅输出此类用例，不输出其他章节

单条用例格式示例：
\`\`\`markdown
#### TC-XXX-001：用例名称
**优先级**：P0

**前置条件：**
- 前置条件1
- 前置条件2

**执行步骤：**
1. 步骤1
2. 步骤2

**预期结果：**
- 预期结果1
- 预期结果2
\`\`\`

请输出完整的改进后测试用例文档（仅用例正文，按功能模块组织）。
`;
}

// ==================== Test Stepwise Generation ====================

/**
 * 生成测试用例功能模块列表（目录）
 */
export function buildTestOutlinePrompt(prd: string, code: string): string {
  return `基于以下 PRD（产品需求文档）和代码实现，生成测试用例的功能模块列表（包含功能模块名称和简短描述）：

${prd ? `## PRD（产品需求文档）：
${prd}

` : ''}## 代码实现：
${code}

要求：
1. **识别所有功能模块**：从 PRD 和代码中识别所有需要测试的功能模块（如：用户管理、订单处理、支付流程等）
2. **按功能模块组织**：每个功能模块应该是一个独立的测试模块
3. **为每个模块生成简短描述**：描述应该：
   - 清晰说明该功能模块的主要功能和职责
   - 明确该模块需要测试的核心功能点
   - 指导测试用例的生成方向和重点
   - 长度控制在1-3句话，简洁明了
4. **输出格式**：每个功能模块包含标题和描述

输出格式示例：
\`\`\`markdown
# 测试用例功能模块列表

## 功能模块1：用户管理
该模块负责用户的注册、登录、信息管理、权限控制等功能。需要测试用户生命周期管理、身份认证、数据安全等核心功能。

## 功能模块2：订单处理
该模块处理订单的创建、查询、修改、取消等操作。需要测试订单状态流转、库存管理、价格计算、异常处理等功能。

## 功能模块3：支付流程
该模块处理支付请求、支付方式选择、支付结果处理等。需要测试支付流程完整性、支付安全、异常回滚、对账等功能。
...
\`\`\`

请生成完整的功能模块列表，确保每个模块都有清晰的名称和描述。`;
}

/**
 * 为指定功能模块生成测试用例
 */
export function buildTestSectionPrompt(
  prd: string,
  code: string,
  outline: string,
  _sectionNumber: number,
  sectionTitle: string,
  sectionDescription?: string
): string {
  return `基于以下 PRD（产品需求文档）、代码实现和功能模块列表，为「${sectionTitle}」功能模块生成详细的测试用例：

${prd ? `## PRD（产品需求文档）：
${prd}

` : ''}## 代码实现：
${code}

## 功能模块列表：
${outline}

## 当前功能模块：${sectionTitle}
${sectionDescription ? `### 功能模块描述：
${sectionDescription}

**重要**：请严格按照上述功能模块描述来生成测试用例，确保测试用例覆盖描述中提到的所有核心功能点和测试重点。

` : ''}测试用例生成要求：
1. **专注于当前功能模块**：只为「${sectionTitle}」功能模块生成测试用例，不要生成其他模块的测试用例
${sectionDescription ? `2. **遵循功能模块描述**：测试用例必须覆盖功能模块描述中提到的所有核心功能点和测试重点
3. **完整的测试用例集**：必须包含以下类型的测试用例：` : `2. **完整的测试用例集**：必须包含以下类型的测试用例：`}
   - 正常流程测试用例（至少2-3个）
   - 边界条件测试用例（至少2-3个）
   - 异常情况测试用例（至少2-3个）
   - 反例测试用例（至少1-2个）

${sectionDescription ? `4. **每个测试用例必须包含**：` : `3. **每个测试用例必须包含**：`}
   - 测试用例名称：清晰描述测试的功能点和场景
   - 前置条件：详细说明测试需要的前置条件和测试数据
   - 执行步骤：详细描述执行的每一步操作
   - 预期结果：明确说明每个验证点的预期结果

${sectionDescription ? `5. **测试用例格式**：` : `4. **测试用例格式**：`}
\`\`\`markdown
## 功能模块：${sectionTitle}

### 功能描述
${sectionDescription || '[简要描述该功能模块的功能和用途]'}

### 测试用例列表

#### 测试用例1：[测试用例名称]
- **前置条件**：
  [详细描述前置条件和测试数据准备]
  
- **执行步骤**：
  [详细描述执行的每一步操作]
  
- **预期结果**：
  [明确说明每个验证点的预期结果]

#### 测试用例2：[测试用例名称]
[按照相同格式填写]
\`\`\`

**格式与禁止项**：执行步骤用 1. 2. 3. 序号列表描述，不要使用 Given、When、Then；预期结果用纯文本列表，不要使用表情符号；只输出本功能模块的测试用例，不要输出「功能实现状态总结」「测试执行计划」「附录」等章节。

请为「${sectionTitle}」功能模块生成完整的测试用例集。${sectionDescription ? `确保测试用例严格遵循功能模块描述中提到的核心功能点和测试重点。` : ''}`;
}

/**
 * 评审指定功能模块的测试用例
 */
export function buildTestSectionReviewPrompt(
  sectionContent: string,
  _sectionNumber: number,
  sectionTitle: string,
  outline: string,
  prd?: string,
  code?: string,
  sectionDescription?: string
): string {
  return `请审查以下「${sectionTitle}」功能模块的测试用例质量：

【功能模块测试用例】
## 功能模块：${sectionTitle}

${sectionContent}

${prd ? `【PRD（产品需求文档）】
${prd}

` : ''}${code ? `【代码实现】
${code}

` : ''}【功能模块列表】
${outline}

${sectionDescription ? `【功能模块描述】
${sectionDescription}

**重要**：请参考上述功能模块描述，检查测试用例是否覆盖了描述中提到的所有核心功能点和测试重点。

` : ''}审查要求：
1. **检查测试用例结构完整性**：每个测试用例是否包含前置条件、执行步骤、预期结果？
2. **检查内容质量**：每个测试用例是否有充实的内容？是否存在空洞、模糊、不可执行的描述？
3. **检查测试覆盖度**：是否包含正常流程、边界、异常、反例测试用例？${sectionDescription ? `是否覆盖功能模块描述中的所有核心功能点？` : ''}
4. **检查可执行性**：测试用例是否足够详细可执行？预期结果是否明确具体？
5. **检查与功能模块的匹配度**：测试用例是否真正覆盖「${sectionTitle}」功能模块的所有功能点？${sectionDescription ? `是否与功能模块描述一致？` : ''}

**禁止输出**：审查范围、审查方法、审查结果总览、详细审查结果等展开章节。仅允许「改进建议」与「审查结论」。

输出格式（仅允许以下两类内容）：
\`\`\`markdown
## 功能模块「${sectionTitle}」测试用例审查报告

### 改进建议
（按测试用例编号/名称列出具体、可执行的改进项）
1. [TC-xxx 或用例名称] 具体改进建议
2. [TC-xxx 或用例名称] 具体改进建议
...

### 审查结论
- 通过 / 需要改进
- 主要问题：（1–3 条摘要）
\`\`\`
`;
}

/**
 * 改进指定功能模块的测试用例
 */
export function buildTestSectionImprovePrompt(
  sectionContent: string,
  reviewReport: string,
  _sectionNumber: number,
  sectionTitle: string,
  outline: string,
  prd?: string,
  code?: string,
  sectionDescription?: string
): string {
  return `请根据以下审查报告的建议，改进和完善「${sectionTitle}」功能模块的测试用例：

【当前功能模块测试用例】
## 功能模块：${sectionTitle}

${sectionContent}

【审查报告】
${reviewReport}

${prd ? `【PRD（产品需求文档）】
${prd}

` : ''}${code ? `【代码实现】
${code}

` : ''}【功能模块列表】
${outline}

${sectionDescription ? `【功能模块描述】
${sectionDescription}

**重要**：改进后的测试用例必须严格遵循功能模块描述，确保覆盖描述中提到的所有核心功能点和测试重点。

` : ''}改进要求：
1. **仔细分析审查报告**：识别所有改进建议和问题点
2. **保持测试用例结构**：每条用例必须包含测试编号、用例名称、前置条件、执行步骤、预期结果、**优先级**（P0/P1/P2）
3. **针对性改进**：补充缺失用例、完善模糊描述、细化步骤与预期结果、补充具体测试数据示例
${sectionDescription ? `   - **确保覆盖功能模块描述**：确保测试用例覆盖描述中提到的所有核心功能点和测试重点` : ''}
4. **内容质量**：所有测试用例必须详细、具体、可执行，可直接用于测试执行
5. **保持格式**：按功能模块组织，与 TEST.md 用例格式一致

**禁止输出**：与用例无关的独立小节（如单独的「功能描述」章节）。若需说明模块用途，可合并为模块标题下的一句话，不单独成节。仅输出测试用例列表，每条含测试编号、用例名称、前置条件、执行步骤、预期结果、优先级。

输出要求与格式：
- 仅输出「${sectionTitle}」功能模块的测试用例列表，每条含：测试编号、用例名称、前置条件、执行步骤、预期结果、**优先级**（P0/P1/P2）
- 不输出「功能描述」等与用例无关的独立小节

\`\`\`markdown
## 功能模块：${sectionTitle}

#### TC-XXX-001：用例名称
**优先级**：P0

**前置条件：**
- 前置条件1

**执行步骤：**
1. 步骤1

**预期结果：**
- 预期结果1

#### TC-XXX-002：用例名称
[按相同格式填写，含优先级]
...
\`\`\`
`;
}

export default {
  TEST_SYSTEM_PROMPT,
  buildTestPrompt,
  TESTABILITY_REVIEW_SYSTEM_PROMPT,
  buildTestabilityReviewPrompt,
  TEST_PLAN_SYSTEM_PROMPT,
  buildTestPlanPrompt,
  AUTOMATION_PLANNING_SYSTEM_PROMPT,
  buildAutomationPlanningPrompt,
  AUTOMATION_EXECUTION_SYSTEM_PROMPT,
  buildAutomationExecutionPrompt,
  COVERAGE_QUALITY_CHECK_SYSTEM_PROMPT,
  buildCoverageQualityCheckPrompt,
  QA_CONCLUSION_SYSTEM_PROMPT,
  buildQAConclusionPrompt,
  TEST_REVIEW_SYSTEM_PROMPT,
  buildTestReviewPrompt,
  TEST_IMPROVE_SYSTEM_PROMPT,
  buildTestImprovePrompt,
  buildTestOutlinePrompt,
  buildTestSectionPrompt,
  buildTestSectionReviewPrompt,
  buildTestSectionImprovePrompt,
};


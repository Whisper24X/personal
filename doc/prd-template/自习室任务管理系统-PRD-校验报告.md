# 自习室任务管理系统 PRD 校验报告（Review Gate）

结论：PASS

阻断项（Blocking Issues）：无

非阻断项（Warnings）：
- `0. 基本信息` 的“相关链接”目前为“立项后输出”，建议补充具体文档链接或至少给出存放位置与冻结版本（章节：`0. 基本信息`；要求：可被研发/测试直接访问与引用）。
- `3.2 核心场景` 覆盖完整，但部分场景未显式写清“收益/结果”，建议按“谁/何时何地/做什么/收益”补齐一句话收益，便于评审对齐（章节：`3.2 核心场景`；要求：每条场景都能对应到 1 个可观测指标或验收点）。

结构对齐结果：

| 章节 | 期望标题 | 是否存在 | 是否一致 | 备注 |
|---|---|---:|---:|---|
| 0 | `## 0. 基本信息（主干 · 全体必读）` | 是 | 是 | 通过 |
| 1 | `## 1. 背景与目标（主干 · 全体必读）` | 是 | 是 | 通过 |
| 2 | `## 2. 范围（主干 · 全体必读）` | 是 | 是 | 通过 |
| 3 | `## 3. 用户与场景（主干 · 全体必读）` | 是 | 是 | 通过 |
| 4 | `## 4. 核心流程（主干 · 全体必读）` | 是 | 是 | 通过 |
| 5 | `## 5. 功能与交互（主干 · 全体必读）` | 是 | 是 | 通过 |
| 6 | `## 6. 业务规则与数据口径（主干 · 全体必读）` | 是 | 是 | 通过 |
| 7 | `## 7. 权限与安全（主干 · 全体必读）` | 是 | 是 | 通过 |
| 8 | `## 8. 异常与边界（主干 · 全体必读）` | 是 | 是 | 通过 |
| 9 | `## 9. 埋点与观测（主干 · 全体必读）` | 是 | 是 | 通过 |
| 10 | `## 10. 验收标准（主干 · 全体必读）` | 是 | 是 | 通过 |
| 11 | `## 11. 变更记录（主干 · 全体必读）` | 是 | 是 | 通过 |

可落地性检查清单：

| 检查项 | 结果 | 证据（原文摘录或小段引用） | 补齐建议（章节/要求） |
|---|---:|---|---|
| A1 MRD 来源可追溯 | ✅ | `MRD 来源 | doc/prd-template/自习室任务管理系统-MRD.md（v1.0，2026-01-08）` | 无 |
| A2 里程碑可执行 | ✅ | `里程碑 | 评审：T+0 周…全量：T+10 周（T 为项目启动周）` | 无 |
| A3 相关链接定位 | ✅ | `接口文档：以本 PRD 第 FE-3/BE-2 为初版；埋点：见第 9 章` | Warning：补齐“原型/视觉稿/看板”的具体链接或文档路径（章节：`0`） |
| B1 目标量化 | ✅ | `学生任务完成率 ≥70%，任务按时完成率 ≥60%` | 无 |
| B2 成功标准可观测 | ✅ | `任务完成率 = …；数据来自任务实例状态与完成时间` | 无 |
| C1 In Scope 可拆分且有优先级 | ✅ | `R1：资源库管理（P0）…R8：可视化看板（P1）` | 无 |
| C2 Out of Scope 明确 | ✅ | `任务完成数据回写 CRM（双向同步）。` | 无 |
| C3 约束与假设覆盖关键不确定点 | ✅ | `假设 CRM 支持“至少每小时一次”的增量同步；若只能人工导入…` | 无 |
| D1 核心场景表达完整 | ✅ | `S1…S5…` | Warning：补齐每条场景的“收益/结果”一句话（章节：`3.2`） |
| D2 主流程可开发 | ✅ | `1. 老师进入资源库…7.（P1）系统按周/月生成…` | 无 |
| D3 分支与回退覆盖关键路径 | ✅ | `计划暂停…恢复后按剩余天数继续发布…` | 无 |
| E1 页面/入口清单完整 | ✅ | `资源库列表 / 模板编排页 / 学生端任务列表 / 数据统计看板 / CRM 同步配置…` | 无 |
| E2 功能清单可拆分 | ✅ | `模块 | 功能点 | 优先级 | 说明 | 负责人` | 无 |
| E3 关键页面状态覆盖完整 | ✅ | `状态：空 / 加载 / 成功 / 失败 / 无权限 / 无数据 / 禁用` | 无 |
| F1 术语与统计口径明确 | ✅ | `完成判定（Completion）：任务实例满足所有要求后标记为完成…` | 无 |
| F2 业务规则可实现可测试 | ✅ | `规则 R-1（发布规则）：… -> …` | 无 |
| F3 状态机迁移条件完整 | ✅ | `迁移条件：到达发布时间 / 学生首次打开… / 当前时间 > 截止时间…` | 无 |
| G1 权限矩阵覆盖核心资源 | ✅ | `学生/老师/教务/管理员` 的读写管理 | 无 |
| G2 安全与风控与风险匹配 | ✅ | `幂等…防重复提交…审计…脱敏…` | 无 |
| H1 异常覆盖要求场景 | ✅ | `网络异常/权限不足/数据为空/并发冲突/外部依赖失败` | 无 |
| H2 异常条目可验证 | ✅ | `触发条件/期望行为/前端提示/可观测性` 列齐 | 无 |
| I1 主链路事件覆盖 | ✅ | `task_list_view` `task_detail_view` `practice_submit` `task_complete` | 无 |
| I2 事件含触发时机与属性 | ✅ | `属性（key:type）` 列有 `resource_id:string` 等 | 无 |
| I3 指标与告警阈值明确 | ✅ | `自动发布失败率 >1%…` | 无 |
| J1 验收语句可测试 | ✅ | `AC-1：…后…可检索到…并可预览播放` | 无 |
| J2 非功能指标明确 | ✅ | `接口 P95 <500ms（不含文件上传）` | 无 |
| J3 灰度/回滚/检查清单可执行 | ✅ | `以班级为粒度灰度…回滚条件…上线检查清单…` | 无 |


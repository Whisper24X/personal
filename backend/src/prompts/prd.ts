/**
 * PRD（产品需求文档）提示词 - 研发&交互可执行增强版
 */

export const PRD_SYSTEM_PROMPT = `
你是一位资深产品经理，长期与前端、后端、测试、交互设计师协作，
擅长输出「研发可直接执行」的产品需求文档（PRD）。

你的目标不是写说明文，而是交付一份可以直接进入研发流程的执行蓝图：
- 研发人员阅读后无需反复追问即可开始开发
- 测试人员可直接基于 PRD 编写测试用例
- 交互设计师可据此还原页面结构与状态

工作原则：
- 所有功能必须明确：触发条件、前置条件、主流程、异常流程、边界条件
- 避免模糊表达（如"尽量""可能""提升体验"等）
- 所有需求必须可验证、可测试、可验收
- 明确需求边界：本期做什么 / 明确不做什么
- 如适用，请区分 MVP 与后续版本规划
- 仅基于 MRD 与上下文推导需求；若必须推断，写入「2.3 约束与假设」并说明影响

输出要求：
- 使用 Markdown
- **必须严格按照提供的 PRD 模板格式输出，保持章节编号和结构完全一致**
- **必须包含模板中的主干章节（0-10章），第11章按需可选但如出现标题必须一致**
- 表格、清单与流程图必须完整填充，不得留空
- 不得保留任何占位符（如 \`<...>\`/TBD/TODO）；信息不足写入「2.3 约束与假设」并说明影响
- 结构清晰、层级合理
- 面向研发与交互团队，而非管理层汇报
`;

export const PRD_TEMPLATE = `# PRD 模板（MRD 自动生成版｜按角色可折叠 / 可分层阅读）

> 使用说明：
> - **适用场景**：用 MRD 作为输入，自动生成 PRD；人只做“审阅/退回重写”，不在 PRD 内协作补写。
> - **生成要求**：生成后的 PRD 中不得保留任何占位符（如 \`<...>\`）、不得出现 \`TBD/TODO\`；信息不足时，必须在「2.3 约束与假设」中写明假设与影响。
> - **主干内容**：所有角色必读（用于评审与范围对齐）
> - **角色关注块**：按需展开（用于设计/实现/测试落地）
> - 折叠建议：在支持 \`details/summary\` 的文档工具中阅读（例如飞书/语雀/Notion/多数 Markdown 渲染器）
> - **不包含**：开放问题 / 决策记录（此类内容不进入自动生成 PRD；如有不确定性，用“约束与假设/风险”表达）

---

## 0. 基本信息

| 项 | 内容 |
|---|---|
| PRD 标题 | \`<产品/功能名>\` |
| PRD 编号 | \`<PRD-001>\` |
| 版本 | \`v0.1\` |
| 状态 | \`草稿 / 评审中 / 已冻结 / 已上线\` |
| MRD 来源 | \`<MRD 标题/版本/链接>\` |
| 负责人 | PM \`<@>\` / UI \`<@>\` / FE \`<@>\` / BE \`<@>\` / QA \`<@>\` |
| 里程碑 | 评审 \`<date>\` / 联调 \`<date>\` / 提测 \`<date>\` / 灰度 \`<date>\` / 全量 \`<date>\` |
| 相关链接 | 原型 \`<link>\` / 视觉稿 \`<link>\` / 接口文档 \`<link>\` / 埋点文档 \`<link>\` / 看板 \`<link>\` |

---

## 1. 背景与目标

### 1.1 背景说明
- 现状与问题：\`<是什么问题/机会，影响谁，影响多大>\`
- 触发原因：\`<为什么现在做>\`

### 1.2 目标（必须可量化）
- 业务目标：\`<例如转化率、留存、客单、成本、时长>\`
- 产品目标：\`<例如效率、可用性、错误率、完成时长>\`

### 1.3 成功标准（Outcome）
- 指标口径与数据来源：\`<指标 = 定义；从哪里采集>\`
- 达标阈值：\`<上线后 N 天达到 X>\`

---

## 2. 范围

### 2.1 需求范围（In Scope）
- [ ] R1：\`<一句话需求>\`（P0/P1/P2）
- [ ] R2：

### 2.2 非目标（Out of Scope）
- \`<明确本期不做什么，避免隐性期望>\`

### 2.3 约束与假设
- 依赖：\`<人/系统/数据/第三方>\`
- 约束：\`<合规/预算/工期/兼容历史>\`
- 假设：\`<如果不成立会怎样>\`

---

## 3. 用户与场景

### 3.1 目标用户
| 用户/角色 | 入口 | 使用频率 | 关键诉求 | 权限/限制 |
|---|---|---|---|---|
| | | | | |

### 3.2 核心场景（建议 3–5 条）
- S1：\`<谁> 在 <何时何地> 想 <做什么> 因为 <收益>\`
- S2：

---

## 4. 核心流程

### 4.1 主流程（Happy Path）
1. \`<步骤 1>\`
2. \`<步骤 2>\`
3. \`<步骤 3>\`

### 4.2 分支与回退（关键）
- 分支：\`<条件 -> 走向>\`
- 回退：\`<取消/撤销/返回/重试>\`

### 4.3 流程图（必填，使用 Mermaid）
\`\`\`mermaid
flowchart TD
  A([开始]) --> B[步骤 1：<动作/页面/接口>]
  B --> C{分支条件：<条件?>}
  C -- 是 --> D[步骤 2A：<动作>]
  C -- 否 --> E[步骤 2B：<动作>]
  D --> F[步骤 3：<动作>]
  E --> F
  F --> G([结束])
\`\`\`

---

## 5. 功能与交互

### 5.1 页面/入口清单
| 页面/路由 | 入口 | 权限 | 说明 |
|---|---|---|---|
| | | | |

### 5.2 功能清单（用于评审与拆分）
| 模块 | 功能点 | 优先级 | 说明 | 负责人 |
|---|---|---|---|---|
| | | P0 | | |

### 5.3 关键页面说明（按页面或任务流拆）
#### \`<页面/流程节点 A>\`
- 目标：\`<用户要完成什么>\`
- 核心操作（按“用户动作 -> 系统反馈”逐条写清楚，可用于实现/测试用例）：
  - O-1：\`<动作名>\`（入口：\`<按钮/手势/快捷键/自动触发>\`；前置：\`<权限/状态/数据>\`；输入：\`<字段/校验/提示>\`；请求：\`<接口/参数/幂等/节流>\`；成功：\`<状态变化/跳转/反馈>\`；失败：\`<错误码/提示/重试>\`；副作用：\`<埋点/日志/通知>\`）
  - O-2：
  - 列表类：\`<搜索/筛选/排序/分页/刷新/加载更多>\`；空态/异常态；操作列（批量/单条）与权限控制
  - 表单类：默认值；校验时机（输入中/失焦/提交）；禁用条件；防重复提交；二次确认/撤销；草稿/离开拦截（如需）
  - 异步与并发：loading 策略；乐观更新/回滚；多端/多窗口冲突处理（如需）
- 文案：\`<关键提示/错误文案，未定可占位>\`
- 状态：空 / 加载 / 成功 / 失败 / 无权限 / 无数据 / 禁用

---

## 6. 业务规则与数据口径

### 6.1 术语与口径
- 术语：\`<名词解释>\`
- 统计口径：\`<例如“新增”如何定义；去重规则；时区>\`

### 6.2 业务规则（必须可实现/可测试）
- 规则 R-1：\`<触发条件> -> <结果>（优先级/例外）\`
- 规则 R-2：

### 6.3 状态机（如有）
| 状态 | 含义 | 允许操作 | 迁移条件 | 备注 |
|---|---|---|---|---|
| | | | | |

---

## 7. 权限与安全

### 7.1 权限矩阵
| 角色 | 资源/页面 | 读 | 写 | 审批/管理 | 备注 |
|---|---|---:|---:|---:|---|
| | | | | | |

### 7.2 安全与风控（按需）
- 幂等：\`<哪些操作必须幂等；幂等键>\`
- 防重复提交：\`<前端/后端策略>\`
- 审计：\`<需要记录哪些操作>\`
- 脱敏：\`<哪些字段需要脱敏>\`

---

## 8. 异常与边界

| 场景 | 触发条件 | 期望行为 | 前端提示/交互 | 可观测性（日志/埋点） |
|---|---|---|---|---|
| 网络异常 | | | | |
| 权限不足 | | | | |
| 数据为空 | | | | |
| 并发冲突 | | | | |
| 外部依赖失败 | | | | |

---

## 9. 埋点与观测

### 9.1 事件定义（最少覆盖主链路）
| event_name | 触发时机 | 属性（key:type） | 用途 |
|---|---|---|---|
| | | | |

### 9.2 指标与告警（按需）
- 核心指标：\`<如成功率、耗时、错误率>\`
- 告警阈值：\`<阈值与负责人>\`

---

## 10. 验收标准

### 10.1 功能验收（可测试、可量化）
- AC-1：当 \`<前置条件>\`，执行 \`<步骤>\`，应得到 \`<期望结果>\`
- AC-2：

### 10.2 非功能指标
- 性能：\`<如接口 P95、首屏、列表渲染>\`
- 兼容：\`<浏览器/设备/系统版本矩阵>\`
- 稳定性：\`<重试/降级/熔断（如有）>\`

### 10.3 上线与回滚
- 灰度策略：\`<开关/人群/比例>\`
- 回滚条件：\`<触发阈值与决策人>\`
- 上线检查清单：\`<核心流程/关键指标/告警>\`

---

## 11. 角色关注块（按需展开）

<details>
<summary><strong>UI 设计师（展开阅读）</strong></summary>

## UI-1 信息架构与页面结构
- 页面层级/导航：\`<信息架构图/页面树>\`
- 模块划分：\`<页面内模块与优先级>\`
- 关键文案：\`<按钮/提示/空态/错误态文案>\`

## UI-2 关键交互与组件规范
- 关键交互：\`<点击/长按/滑动/拖拽/快捷键/动效>\`
- 组件清单：\`<复用组件/新增组件/变体>\`
- 状态覆盖：正常 / 悬停 / 按下 / 禁用 / 加载 / 空 / 错误 / 无权限

## UI-3 视觉与适配
- 视觉方向：\`<品牌/色板/字体/间距/暗色模式>\`
- 多端适配：\`<Web/移动/平板；断点策略>\`
- 无障碍：\`<对比度/字号/可聚焦/读屏>\`

## UI-4 交付与验收
- 交付物：视觉稿 \`<link>\` / 标注 \`<link>\` / 组件库更新 \`<link>\`
- 验收口径：交互一致 / 状态齐全 / 适配通过

</details>

<details>
<summary><strong>前端工程师（展开阅读）</strong></summary>

## FE-1 路由、页面拆分与权限
- 路由：\`<path/参数/重定向>\`
- 页面拆分：\`<模块边界/复用组件>\`
- 权限：\`<鉴权点/无权限落地页/按钮级权限>\`

## FE-2 状态与交互逻辑（边界必须明确）
- 状态机：\`<核心状态与转换条件>\`
- 表单校验：\`<规则/触发时机/提示文案>\`
- 异常策略：\`<超时/重试/兜底/离线>\`

## FE-3 接口契约（联调必备）
- 接口列表（逐条列出）：
  - \`<接口名>\`：\`<METHOD> <URL>\`
    - 请求：\`字段/类型/必填/枚举/分页/排序/过滤\`
    - 响应：\`字段/类型/枚举/默认值\`
    - 错误码：\`code -> 前端文案/处理策略\`
- 约定：\`幂等/重试/超时/版本兼容/降级字段\`

## FE-4 埋点、性能与兼容
- 埋点对齐：\`event_name/触发时机/属性\`
- 性能目标：\`首屏/TTI/包体/列表渲染\` 与验证方式
- 兼容矩阵：\`浏览器/系统/设备\`

## FE-5 发布与回滚
- 开关：\`<feature flag>\`
- 灰度：\`<分组/比例/回滚动作>\`

</details>

<details>
<summary><strong>后端工程师（展开阅读）</strong></summary>

## BE-1 领域模型与数据模型
- 领域对象：\`<实体/值对象/关系>\`
- 数据表：\`<表/字段/索引/约束/迁移>\`
- 一致性：\`<事务边界/最终一致/补偿>\`

## BE-2 API 设计与规范
- 接口列表（逐条列出）：
  - \`<接口名>\`：\`<METHOD> <URL>\`
    - 鉴权：\`<方式/角色/权限点>\`
    - 幂等：\`<幂等键/语义>\`
    - 限流：\`<策略>\`
    - 错误码：\`<规范与示例>\`
- 兼容性：\`<版本/灰度字段/默认值/兼容旧客户端>\`

## BE-3 业务规则实现（必须可测试）
- 校验：\`<服务端强校验清单>\`
- 计算：\`<公式/精度/舍入>\`
- 并发：\`<锁/去重/冲突策略>\`

## BE-4 性能、可靠性与安全
- 性能目标：\`<QPS/延迟/SLA>\`
- 异步任务：\`<队列/重试/死信/补偿>\`
- 安全：\`<审计/脱敏/风控>\`
- 可观测性：\`<日志字段/指标/告警阈值>\`

## BE-5 上线与回滚
- 数据迁移：\`<步骤/回滚策略>\`
- 灰度与回滚：\`<开关/脚本/回滚动作>\`

</details>

<details>
<summary><strong>测试工程师（展开阅读）</strong></summary>

## QA-1 测试范围与风险
- 范围：\`<测什么/不测什么>\`
- 风险点：\`<高风险流程/历史缺陷/复杂规则>\`

## QA-2 用例设计（建议表格输出到测试用例库）
| 用例 | 前置条件 | 步骤 | 期望结果 | 优先级 | 类型（冒烟/回归/探索） |
|---|---|---|---|---|---|
| | | | | | |

## QA-3 环境、数据与账号
- 环境：\`dev/stage/prod-like\`
- 账号与权限：\`<角色矩阵>\`
- 数据准备：\`<脚本/初始化/清理>\`

## QA-4 接口与错误码校验
- 契约核对：字段/类型/枚举/默认值/分页排序
- 错误码：触发条件与前端提示一致性

## QA-5 上线验证与回滚演练
- 上线后检查：核心链路、关键指标、告警
- 回滚触发：\`<阈值/负责人/操作步骤>\`

</details>
`;

/**
 * 知识输入引用（CLI 模式使用）
 * 指示 CLI 参考工作目录中的历史文档和代码
 */
export const KNOWLEDGE_INPUT_REFERENCE_PRD = `
【重要：知识输入】
请参考以下目录中的历史文档和代码作为知识输入（这些是重要依据）：

1. 归档历史文档（docs-archive/）：
   - docs-archive/prd/: 历史 PRD 文档版本（保持一致性）
   - docs-archive/mrd/: 历史 MRD 文档版本

2. 当前 MRD 文档：
   - docs/mrd/: 当前正在生成的 MRD 文档

3. 业务知识库（docs/business-knowledge/）：
   - 业务方上传的产品规范、业务流程等知识文档
   - 这些是重要的业务背景和约束条件

4. 代码实现：
   - ainative-app/src/: 移动端代码实现（了解现有功能）
   - ainative-backend/: 后端 API 和业务逻辑（了解接口规范）
   - ainative-shadow/src/: 管理后台功能（了解管理功能）
   - ainative-pc/src/: PC端代码实现

5. 开发规范（docs/dev-spec/）：
   - 各子项目的开发规范和架构说明

【功能冲突检测】
如果新功能与现有实现存在冲突，请在 PRD 中明确指出：
- 冲突点描述
- 需要修改的现有功能
- 兼容性考虑
- 建议解决方案
`;

export function buildPRDPrompt(input: string): string {
  return `基于以下市场研究文档（MRD），生成一份【研发与交互可直接执行】的产品需求文档（PRD）：

【市场研究文档（MRD）】
${input}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

生成要求：
1. **严格遵循 PRD 模板结构**：主干 0-10 章必须齐全；第 11 章按需可选但如出现标题必须一致；不要新增无关的 \`##\` 级章节（模板内角色关注块除外）
2. **填充模板中的所有表格、清单、流程图**，不得留空或仅保留表头
3. **不得出现占位符或未完成标记**（如 \`<...>\`/TBD/TODO/待补充）
4. **信息不足必须写入「2.3 约束与假设」**，说明假设、影响与兜底
5. **4.3 必须提供 Mermaid 流程图**，且与 4.1/4.2 的文字流程一致
6. **5.3 关键页面说明按“用户动作 -> 系统反馈”展开**，覆盖入口、前置、输入校验、请求、成功/失败、异常、埋点/日志，并覆盖页面状态
7. **6-10 章节必须可验证**：规则、权限、安全、异常、埋点、验收、上线回滚可测试/可观测
8. 避免模糊表述；如需合理推断，明确标注为假设

硬性要求：
- 输出完整 Markdown PRD，必须包含模板主干章节（0-10章）
- 内容不少于 3000 字
- 不保留任何占位符或空表格/空清单
- 章节编号和标题必须与模板完全一致
- 10.1 验收标准必须使用可测试语句（Given/When/Then 或等价形式）
`;
}

/**
 * 构建带知识输入引用的 PRD Prompt（CLI 模式使用）
 * 包含对历史文档和代码的引用指令
 * 
 * @param input MRD 内容
 * @returns 带知识输入引用的完整 prompt
 */
export function buildPRDPromptWithKnowledge(input: string): string {
  return `${KNOWLEDGE_INPUT_REFERENCE_PRD}

基于以下市场研究文档（MRD），生成一份【研发与交互可直接执行】的产品需求文档（PRD）：

【市场研究文档（MRD）】
${input}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

生成要求：
1. **严格遵循 PRD 模板结构**：主干 0-10 章必须齐全；第 11 章按需可选但如出现标题必须一致；不要新增无关的 \`##\` 级章节（模板内角色关注块除外）
2. **参考知识输入**：参考上述目录中的历史文档和代码，了解现有功能和设计
3. **功能冲突检测**：如果新功能与现有实现冲突，请明确指出并提供解决方案
4. **填充模板中的所有表格、清单、流程图**，不得留空或仅保留表头
5. **不得出现占位符或未完成标记**（如 \`<...>\`/TBD/TODO/待补充）
6. **信息不足必须写入「2.3 约束与假设」**，说明假设、影响与兜底
7. **4.3 必须提供 Mermaid 流程图**，且与 4.1/4.2 的文字流程一致
8. **5.3 关键页面说明按"用户动作 -> 系统反馈"展开**，覆盖入口、前置、输入校验、请求、成功/失败、异常、埋点/日志，并覆盖页面状态
9. **6-10 章节必须可验证**：规则、权限、安全、异常、埋点、验收、上线回滚可测试/可观测
10. 避免模糊表述；如需合理推断，明确标注为假设

硬性要求：
- 输出完整 Markdown PRD，必须包含模板主干章节（0-10章）
- 内容不少于 3000 字
- 不保留任何占位符或空表格/空清单
- 章节编号和标题必须与模板完全一致
- 10.1 验收标准必须使用可测试语句（Given/When/Then 或等价形式）
- 如果发现与现有功能的冲突，必须在「2.3 约束与假设」或相关章节中明确说明
`;
}

export function buildPRDUpdatePrompt(historyPRD: string, newRequirements: string): string {
  return `基于以下历史 PRD 与新增需求，生成更新后的完整 PRD 文档：

【历史 PRD】
${historyPRD}

【新增 / 变更需求】
${newRequirements}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

更新规则：
1. **严格遵循 PRD 模板结构**：主干 0-10 章必须齐全；第 11 章按需可选但如出现标题必须一致
2. **必须保持与模板完全一致的章节编号和层级结构**
3. 未被新需求影响的内容需完整保留
4. 变更内容需明确标注（新增 / 修改 / 删除）
5. 在文档顶部（## 0. 基本信息）补充版本更新说明（版本、状态、变更摘要）
6. 信息不足或新增假设写入「2.3 约束与假设」，说明影响与兜底

输出要求：
- 完整 Markdown PRD，必须包含模板主干章节（0-10章）
- 每个功能仍需满足研发级描述标准
- 内容不少于 3000 字
- 章节编号和标题必须与模板完全一致
- 交互细节必须详细具体，包含输入校验与错误提示
- 不保留任何占位符或空表格/空清单
`;
}

export function buildPRDUpdateWithRAGPrompt(
  historyPRD: string,
  relevantChunks: string,
  newRequirements: string,
  query?: string
): string {
  const queryPart = query ? `\n\n【用户关注点】\n${query}` : '';

  return `基于以下历史 PRD、检索到的相关内容与新增需求，生成更新后的完整 PRD 文档：

【历史 PRD】
${historyPRD}

【检索到的历史PRD内容】
${relevantChunks}${queryPart}

【新增 / 变更需求（MRD）】
${newRequirements}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

更新规则：
1. **严格遵循 PRD 模板结构**：主干 0-10 章必须齐全；第 11 章按需可选但如出现标题必须一致
2. **必须保持与模板完全一致的章节编号和层级结构**
3. 未被新需求影响的内容需完整保留
4. 变更内容需明确标注（新增 / 修改 / 删除）
5. 在文档顶部（## 0. 基本信息）补充版本更新说明（版本、状态、变更摘要）
6. 信息不足或新增假设写入「2.3 约束与假设」，说明影响与兜底
7. 保持与历史 PRD 一致性，如存在冲突以新需求为准并说明原因

输出要求：
- 完整 Markdown PRD，必须包含模板主干章节（0-10章）
- 每个功能仍需满足研发级描述标准
- 内容不少于 3000 字
- 章节编号和标题必须与模板完全一致
- 交互细节必须详细具体，包含输入校验与错误提示
- 不保留任何占位符或空表格/空清单
`;
}

export function buildPRDWithRAGPrompt(
  query: string,
  relevantChunks: string,
  newRequirements: string
): string {
  return `请基于以下历史 PRD 片段与新的市场研究文档（MRD），生成或更新完整 PRD：

【检索到的历史PRD内容】
${relevantChunks}

【用户关注点】
${query}

【新的市场研究文档（MRD）】
${newRequirements}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

生成要求：
1. **严格遵循 PRD 模板结构**：主干 0-10 章必须齐全；第 11 章按需可选但如出现标题必须一致
2. **必须保持与模板完全一致的章节编号和层级结构**
3. 保持与历史 PRD 的一致性
4. 如存在冲突，以新需求为准并说明原因
5. 所有功能需满足研发与测试可直接使用标准
6. 信息不足必须写入「2.3 约束与假设」，说明假设与影响

输出要求：
- 完整 Markdown PRD，必须包含模板主干章节（0-10章）
- 内容不少于 3000 字
- 章节编号和标题必须与模板完全一致
- 交互细节必须详细具体
- 不保留任何占位符或空表格/空清单
`;
}

/**
 * 生成 PRD 目录（仅输出章节结构）
 */
export function buildPRDOutlinePrompt(input: string): string {
  return `基于以下市场研究文档（MRD），生成 PRD 的目录结构（仅输出章节标题，不输出内容）：

【市场研究文档（MRD）】
${input}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

要求：
1. **严格按照模板结构输出目录，包含所有主干章节（## 0. 到 ## 10.）**
2. 第 11 章按需可选，但如输出标题必须与模板一致
3. **只输出 Markdown 格式的目录，格式如下：**
\`\`\`
## 0. 基本信息
## 1. 背景与目标
## 2. 范围
...
## 10. 验收标准
\`\`\`
4. 如需包含第 11 章，请单独追加一行：\`## 11. 角色关注块（按需展开）\`
5. 不要输出任何章节内容，只输出章节标题
6. 保持章节编号和标题与模板完全一致

输出格式：纯 Markdown 目录，每个章节一行
`;
}

/**
 * 生成单个章节的详细内容
 */
export function buildPRDSectionPrompt(
  input: string,
  outline: string,
  sectionNumber: number,
  sectionTitle: string
): string {
  const sectionTemplate = extractSectionTemplate(PRD_TEMPLATE, sectionNumber);

  return `基于以下市场研究文档（MRD）和 PRD 目录，生成第 ${sectionNumber} 章「${sectionTitle}」的详细内容：

【市场研究文档（MRD）】
${input}

【PRD 目录】
${outline}

【PRD 模板（目标章节，必须严格对齐结构与格式）】
${sectionTemplate || '（未找到模板章节，请严格对齐既定模板结构）'}

【目标章节】
## ${sectionNumber}. ${sectionTitle}

生成要求：
1. **严格按照 PRD 模板中第 ${sectionNumber} 章的结构和格式输出**
2. **必须包含并填充该章节下的所有子章节、表格与清单**
3. 内容要详细、具体、可执行，避免空洞和占位符
4. 若章节涉及流程/功能/规则/验收，请补齐触发条件、前置条件、主流程、异常流程、边界条件、验收标准
5. 若章节为 2.3，必须写明假设及其影响；若章节为 4.3，必须输出 Mermaid 流程图
6. 内容要面向研发和测试团队，确保可直接使用

输出要求：
- 只输出第 ${sectionNumber} 章的内容（包含章节标题）
- 内容要充实，不少于 500 字
- 使用 Markdown 格式
 - 不保留任何占位符或空表格/空清单
`;
}

function extractSectionTemplate(template: string, sectionNumber: number): string {
  const lines = template.split('\n');
  const startPattern = new RegExp(`^##\\s+${sectionNumber}\\.\\s+`);
  const sectionHeaderPattern = /^##\s+\d+\.\s+/;

  const startIndex = lines.findIndex((line) => startPattern.test(line));
  if (startIndex === -1) {
    return '';
  }

  let endIndex = lines.length;
  for (let i = startIndex + 1; i < lines.length; i++) {
    if (sectionHeaderPattern.test(lines[i])) {
      endIndex = i;
      break;
    }
  }

  return lines.slice(startIndex, endIndex).join('\n').trim();
}

/**
 * PRD Review 提示词
 */
export const PRD_REVIEW_SYSTEM_PROMPT = `你是一位资深的产品文档审查专家，擅长检查 PRD 文档的完整性和质量。

你的职责是：
- 只基于 PRD 原文审查，不补写 PRD 内容
- 检查 PRD 是否包含所有必需的主干章节（0-10章），第11章如出现标题是否一致
- 检查每个章节的内容是否充实、具体、可验证
- 识别空洞、模糊、占位符或空表格/空清单内容
- 检查 2.3 是否明确假设与影响
- 检查 4.3 Mermaid 流程图是否与文字流程一致
- 检查 5.3 关键页面说明是否覆盖状态与交互细节
- 检查 9 章埋点与观测是否覆盖主链路
- 检查 10 章验收与上线回滚是否可执行
- 提供可落地的改进建议

输出格式：结构化的审查报告`;

export function buildPRDReviewPrompt(prdContent: string, outline: string): string {
  return `请审查以下 PRD 文档的质量：

【PRD 文档】
${prdContent}

【预期目录结构】
${outline}

审查要求：
1. **检查章节完整性**：是否包含所有主干章节（## 0. 基本信息 到 ## 10. 验收标准）？第 11 章如出现标题是否一致？
2. **检查内容质量**：每个章节是否有充实的内容？是否存在空洞、模糊、占位符或空表格/空清单？
3. **检查格式规范**：章节编号、标题是否与模板一致？是否新增无关 \`##\` 级章节（模板角色关注块除外）？
4. **检查关键落地要素**：2.3 假设与影响、4.3 Mermaid 流程图、5.3 关键页面说明、9 章埋点、10 章验收与上线回滚是否完整？
5. **检查可执行性**：功能/流程/规则/验收是否可测试、可验证？

输出格式：
\`\`\`markdown
# PRD 审查报告

## 1. 章节完整性检查
- [ ] 章节 0. 基本信息：存在/缺失
- [ ] 章节 1. 背景与目标：存在/缺失
- [ ] 章节 2. 范围：存在/缺失
- [ ] 章节 3. 用户与场景：存在/缺失
- [ ] 章节 4. 核心流程：存在/缺失（需检查 4.3 Mermaid）
- [ ] 章节 5. 功能与交互：存在/缺失（需检查 5.3 关键页面说明）
- [ ] 章节 6. 业务规则与数据口径：存在/缺失
- [ ] 章节 7. 权限与安全：存在/缺失
- [ ] 章节 8. 异常与边界：存在/缺失
- [ ] 章节 9. 埋点与观测：存在/缺失
- [ ] 章节 10. 验收标准：存在/缺失
- [ ] 章节 11. 角色关注块（按需展开）：存在/缺失（可选）

## 2. 内容质量检查
### 发现的问题：
1. [章节编号] 问题描述
2. [章节编号] 问题描述

### 空洞内容识别：
- [章节编号] 具体位置和问题

### 关键落地性检查：
- [ ] 2.3 是否写明假设及其影响？
- [ ] 4.3 是否提供 Mermaid 流程图且与文字流程一致？
- [ ] 5.3 是否覆盖关键页面状态与交互细节（含校验与提示文案）？
- [ ] 9.1 是否覆盖主链路事件且包含触发时机与属性？
- [ ] 10.1 是否为可测试语句（Given/When/Then 或等价形式）？

## 3. 改进建议
1. 建议 1
2. 建议 2

## 4. 审查结论
- 通过 / 需要改进
- 主要问题：[列出主要问题]
\`\`\`
`;
}

/**
 * 审核单个章节的提示词
 */
export function buildPRDSectionReviewPrompt(
  sectionContent: string,
  sectionNumber: number,
  sectionTitle: string,
  outline: string
): string {
  const sectionTemplate = extractSectionTemplate(PRD_TEMPLATE, sectionNumber);

  return `请审查以下 PRD 章节的质量：

【章节内容】
## ${sectionNumber}. ${sectionTitle}

${sectionContent}

【PRD 模板（目标章节，必须严格对齐结构与格式）】
${sectionTemplate || '（未找到模板章节，请严格对齐既定模板结构）'}

【完整目录结构】
${outline}

审查要求：
1. **检查内容充实度**：章节内容是否充实、具体？是否存在空洞、模糊或占位符？
2. **检查格式规范**：章节编号、标题是否与目录一致？
3. **检查可执行性**：如涉及流程/功能/规则/验收，是否包含触发条件、主流程、异常流程、边界条件、验收标准？
4. **检查关键元素**：如为 2.3 是否写明假设与影响；如为 4.3 是否提供 Mermaid；如为 5.3 是否覆盖状态与交互细节
5. **检查完整性**：章节是否包含该章节应该包含的所有子章节、表格与清单？
6. **检查模板一致性**：子标题、表格结构、清单项是否与模板一致？有无遗漏或新增无关结构？

输出格式：
\`\`\`markdown
## 章节 ${sectionNumber}. ${sectionTitle} 审查结果

### 1. 内容质量
- [ ] 内容充实：是/否
- [ ] 内容具体：是/否
- [ ] 无占位符或空表格/空清单：是/否

### 2. 发现的问题
1. 问题描述（具体位置）
2. 问题描述（具体位置）

### 3. 改进建议
1. 建议 1
2. 建议 2

### 4. 审查结论
- 通过 / 需要改进
- 主要问题：[列出主要问题]
\`\`\`
`;
}

/**
 * 调整单个章节的提示词
 */
export function buildPRDSectionAdjustPrompt(
  originalSectionContent: string,
  sectionNumber: number,
  sectionTitle: string,
  userRequest: string,
  fullPRDContext?: string,
  conversationHistory?: string
): string {
  const contextPart = fullPRDContext
    ? `\n\n【完整 PRD 上下文（仅供参考）】\n${fullPRDContext}`
    : '';
  
  const historyPart = conversationHistory || '';

  return `请根据用户的要求，调整以下 PRD 章节的内容：

【原始章节内容】
## ${sectionNumber}. ${sectionTitle}

${originalSectionContent}
${historyPart}

【用户调整要求】
${userRequest}
${contextPart}

调整要求：
1. **保持章节标题格式不变**：## ${sectionNumber}. ${sectionTitle}
2. **根据用户要求调整章节内容**，确保调整后的内容：
   - 符合 PRD 模板中该章节的结构要求
   - 保持与整体 PRD 的一致性
   - 内容详细、具体、可执行
   - 面向研发和测试团队
   - 不保留占位符或空表格/空清单
   - ${historyPart ? '**考虑之前的对话历史，保持调整的连贯性**' : ''}
3. **只输出调整后的章节内容**（包含章节标题）
4. **不要输出其他章节的内容**
5. 如果用户要求与 PRD 模板结构冲突，优先满足用户要求，但保持格式规范

输出格式：Markdown 格式的章节内容
`;
}

/**
 * 根据审查报告改进PRD文档的提示词
 */
export const PRD_IMPROVE_SYSTEM_PROMPT = `# PRD 校验提示词（Review Gate）

你是“PRD 质量审查官（Review Gate）”。你的唯一目标是：对输入 PRD 做**严格结构校验**与**可落地性校验**，判断它是否满足评审进入研发的最低门槛。

校验基准模板：\`doc/prd-template/prd-template.md\`（MRD 自动生成版）。

## 输入

我会提供一份 PRD 全文（Markdown）。

约束：
- 只允许基于 PRD 原文进行判断；**不要补写 PRD、不要脑补缺失信息**。
- “建议补齐内容”必须以“需要补充什么 + 放在哪个章节 + 需要达到什么可验收程度”的形式表达。
- 不要输出与校验无关的长篇解释。

## 输出（格式必须严格遵守）

请按以下结构输出：

1) \`结论：PASS\` 或 \`结论：FAIL\`
2) \`阻断项（Blocking Issues）\`：若 FAIL，列出所有阻断项；若 PASS，写 \`无\`
3) \`非阻断项（Warnings）\`：可选优化项；没有写 \`无\`
4) \`结构对齐结果\`：用表格列出模板每一章（0–10 必选，11 可选）是否存在、标题是否一致
5) \`可落地性检查清单\`：用表格逐项打勾/打叉，并给出定位与补齐建议

无论 PASS/FAIL，必须完整输出第 4/5 项表格与证据。

输出示例（仅示意结构，不要照抄内容）：
- 结论：FAIL
- 阻断项：…
- 非阻断项：…
- 结构对齐结果：| 章节 | 期望标题 | 是否存在 | 是否一致 | 备注 |
- 可落地性检查清单：| 检查项 | 结果 | 证据（原文摘录或小段引用） | 补齐建议（章节/要求） |

## 判定规则（硬性）

出现任一条即 **FAIL**：
- 未严格按模板章节结构输出：主干 0–10 缺章/跳号/标题不一致（以 \`##\` 级标题为准）；如出现 \`## 11. 角色关注块（按需展开）\`，其标题也必须一致
- 出现任何占位符或未完成标记：包括但不限于 \`<...>\`、\`TBD\`、\`TODO\`、\`待补充\`、\`占位\`
- 主干内容无法评审落地：范围/流程/规则/验收/埋点/上线回滚 任一项缺失或不可验证
- 验收标准不可测试：只有口号，没有 Given/When/Then 或可执行用例表达
- 关键约束信息缺失且未在「2.3 约束与假设」中显式声明假设与影响

出现以下情况允许 PASS，但需给出 Warning：
- 信息完整但表达啰嗦、重复、可读性差（不影响研发落地）
- 个别表格为空但章节内其他形式给出了同等信息且可验证

## 结构对齐校验（必须逐项检查）

PRD 必须包含以下主干章节（\`##\` 级标题必须一致；章节顺序与编号不得乱；允许章节内新增 \`###\` 小节）：
- \`## 0. 基本信息\`
- \`## 1. 背景与目标\`
- \`## 2. 范围\`
- \`## 3. 用户与场景\`
- \`## 4. 核心流程\`
- \`## 5. 功能与交互\`
- \`## 6. 业务规则与数据口径\`
- \`## 7. 权限与安全\`
- \`## 8. 异常与边界\`
- \`## 9. 埋点与观测\`
- \`## 10. 验收标准\`

说明：
- \`## 11. 角色关注块（按需展开）\` 为可选章节：若出现，其标题必须一致；若缺失不作为结构性阻断。
- 角色关注块内可能包含 \`## UI-\` / \`## FE-\` / \`## BE-\` / \`## QA-\` 等 \`##\` 级标题（通常位于 \`<details>...</details>\` 内）；这些不作为“结构不一致”的阻断项，但若存在也不得包含占位符。
- 若出现与模板无关的其他 \`##\` 级章节标题，记为 Warning（除非造成主干缺章/跳号/标题不一致）。
- PRD **不应**包含“开放问题/决策记录”章节；如出现，记为 Warning（除非导致章节结构不一致）。

## 可落地性检查清单（必须逐项给结论 + 证据）

你必须检查以下项目，并在表格中逐项给出结果（✅/❌）：

### A. 基本信息可追溯
- \`PRD 标题/编号/版本/状态/负责人\` 是否完整可追溯（缺失则阻断）
- \`MRD 来源\` 是否填写且可追溯（标题/版本/链接至少其一明确）
- 里程碑是否具备可执行时间点（允许日期为空但需要“相对时间 + 触发条件”，否则记阻断）
- 相关链接缺失不阻断，但要标 Warning（若 PRD 依赖原型/接口/埋点却未给出任何定位方式则阻断）

### B. 目标可量化、成功标准可观测
- 目标是否量化（至少一个可衡量指标 + 口径/来源）
- 成功标准是否可观测（能在埋点/日志/指标中验证）

### C. 范围清晰且可控
- In Scope 是否包含可拆分的条目，并标注优先级（P0/P1/P2）
- Out of Scope 是否明确（避免隐性期望）
- 约束与假设是否覆盖关键不确定点（并给出“若假设不成立的影响/兜底”）

### D. 场景与流程可实现
- 核心场景（3–5）是否完整表达“谁/何时何地/做什么/收益”
- 主流程是否足够细到可开发（可映射到页面/接口/规则）
- 分支与回退是否覆盖关键异常路径（取消/撤销/重试/返回等）
- Mermaid 流程图是否存在且可读（步骤/分支与文字流程一致；缺失则阻断）

### E. 功能与交互可评审
- 页面/入口清单是否完整（至少包含关键入口、权限、说明）
- 功能清单是否可用于拆分任务（模块、功能点、优先级、负责人或可选）
- 关键页面说明是否覆盖状态：空/加载/成功/失败/无权限/无数据/禁用

### F. 规则/口径可编码、可测试
- 术语与统计口径是否明确（新增/去重/时区等）
- 业务规则是否可实现（条件->结果，含例外/优先级）
- 若有状态机，迁移条件是否完整；若无但业务存在状态流转，需提示补充（阻断或 Warning 视影响而定）

### G. 权限与安全可落地
- 权限矩阵是否覆盖核心资源/页面（读/写/管理）
- 幂等/防重复/审计/脱敏等是否与业务风险匹配（缺失且存在高风险操作则阻断）

### H. 异常与边界可验证
- 异常表格是否覆盖：网络异常/权限不足/数据为空/并发冲突/外部依赖失败
- 每条异常是否包含：触发条件、期望行为、前端提示、可观测性

### I. 埋点与观测可闭环
- 主链路事件是否覆盖（至少能复盘：进入->关键操作->成功/失败）
- 每个事件是否给出触发时机与属性（key:type）
- 是否给出核心指标/告警阈值或至少说明“如何定义/从哪里取”（缺失且影响上线监控则阻断）

### J. 验收、发布、回滚可执行
- 功能验收是否以可测试语句表达（Given/When/Then 或等价形式）
- 非功能指标是否明确（性能/兼容/稳定性）
- 灰度策略、回滚条件、上线检查清单是否可执行（缺失则阻断）

## 引用规则（证据）

证据引用必须满足：
- 只引用 PRD 原文的短片段（1–3 行）或关键句，不要贴大段原文
- 每个阻断项必须提供至少 1 处证据引用或明确指出“整章缺失”

## 语言与风格

- 输出用中文
- 用“可落地、可验证”的标准做判断，不用“感觉/主观好坏”
- 若 FAIL：优先列出最少必要的补齐动作（让下一轮生成可以直接通过）而不是泛泛而谈
`;

export function buildPRDImprovePrompt(
  prdContent: string,
  reviewReport: string
): string {
  return `请根据以下审查报告的建议，改进和完善PRD文档：

【当前PRD文档】
${prdContent}

【审查报告】
${reviewReport}

改进要求：
1. **仔细分析审查报告**：识别所有改进建议和问题点
2. **保持文档结构**：不要改变章节编号和标题，只改进内容（主干 0-10 章必须完整）
3. **针对性改进**：
   - 补充缺失的章节内容
   - 完善简略或模糊的描述
   - 补充功能/流程/规则/验收的触发条件、异常流程、边界条件等
   - 细化交互细节和错误提示文案
   - 补充「2.3 约束与假设」中的假设与影响
   - 补充「4.3 流程图」「9 埋点与观测」「10 验收标准」等关键落地要素
4. **内容质量**：
   - 所有内容必须详细、具体、可执行
   - 避免空洞、模糊或占位符内容
   - 确保研发和测试团队可直接使用
5. **保持格式**：使用Markdown格式，保持章节层级清晰

输出要求：
- 输出完整的改进后的PRD文档
- 保持所有主干章节（## 0. 到 ## 10.），第 11 章按需可选
- 确保改进后的内容解决了审查报告中提出的所有问题
- 不保留任何占位符或空表格/空清单
- 如果审查报告建议增加新章节，可以在现有章节基础上扩展，但保持主要结构不变
`;
}

/**
 * 构建单个章节改进提示词
 * 用于分章节改进，避免 LLM 输出被截断
 */
export function buildPRDSectionImprovePrompt(
  sectionContent: string,
  sectionNumber: number,
  sectionTitle: string,
  sectionReview: string
): string {
  return `请根据以下审查建议，改进和完善PRD文档的第 ${sectionNumber} 章节：

【当前章节内容】
${sectionContent}

【该章节的审查建议】
${sectionReview}

改进要求：
1. **针对性改进**：根据审查建议改进该章节内容
2. **保持章节标题**：必须保持 "## ${sectionNumber}. ${sectionTitle}" 作为章节标题
3. **内容质量**：
   - 补充缺失或不完整的内容
   - 完善简略或模糊的描述
   - 确保内容详细、具体、可执行
   - 避免空洞、模糊或占位符内容
4. **格式要求**：
   - 使用Markdown格式
   - 保持层级结构清晰
   - 表格、清单必须完整填充

输出要求：
- 只输出改进后的该章节完整内容（包括章节标题）
- 不要输出其他章节的内容
- 不要添加额外的说明或注释
- 确保解决了审查建议中提出的问题
`;
}

/**
 * 整体审核提示词（用于分章节审核后的合并文档审核）
 * 侧重于跨章节一致性、整体连贯性和文档完整性
 */
export function buildPRDFullReviewPrompt(prdContent: string, outline: string): string {
  return `请对以下合并后的完整 PRD 文档进行整体审核：

【PRD 文档】
${prdContent}

【目录结构】
${outline}

**审核重点**（各章节已单独审核，本次重点关注整体性）：

1. **跨章节一致性**：
   - 术语定义是否在各章节中保持一致？
   - 功能描述与流程定义是否相互对应？
   - 权限设置与功能要求是否匹配？
   - 异常处理与业务规则是否一致？

2. **整体连贯性**：
   - 章节之间的逻辑顺序是否合理？
   - 信息是否有重复或矛盾？
   - 前后引用是否正确？

3. **完整性检查**：
   - 是否遗漏任何主干章节（0-10章）？
   - 关键流程是否完整覆盖？
   - 验收标准是否覆盖所有功能点？

4. **可执行性评估**：
   - 研发人员是否可以直接基于此文档开发？
   - 测试人员是否可以直接编写测试用例？
   - 是否还有需要澄清的模糊点？

输出格式：
\`\`\`markdown
# PRD 整体审核结果

## 1. 跨章节一致性
- [ ] 术语一致性：通过/存在问题
- [ ] 功能与流程对应：通过/存在问题
- [ ] 权限与功能匹配：通过/存在问题
- [ ] 异常与规则一致：通过/存在问题

### 发现的不一致问题：
1. [问题描述]
2. [问题描述]

## 2. 整体连贯性
- [ ] 章节逻辑顺序：合理/需调整
- [ ] 信息重复或矛盾：无/存在
- [ ] 前后引用正确性：正确/存在问题

### 发现的连贯性问题：
1. [问题描述]
2. [问题描述]

## 3. 完整性评估
- [ ] 主干章节完整：是/否
- [ ] 流程覆盖完整：是/否
- [ ] 验收标准覆盖：完整/不完整

### 遗漏内容：
1. [遗漏描述]
2. [遗漏描述]

## 4. 改进建议
1. [建议 1]
2. [建议 2]

## 5. 审查结论
- 通过 / 需要改进
- 总体评价：[简要评价]
\`\`\`
`;
}

export default {
  PRD_SYSTEM_PROMPT,
  PRD_TEMPLATE,
  KNOWLEDGE_INPUT_REFERENCE_PRD,
  buildPRDPrompt,
  buildPRDPromptWithKnowledge,
  buildPRDUpdatePrompt,
  buildPRDUpdateWithRAGPrompt,
  buildPRDWithRAGPrompt,
  buildPRDOutlinePrompt,
  buildPRDSectionPrompt,
  PRD_REVIEW_SYSTEM_PROMPT,
  buildPRDReviewPrompt,
  buildPRDSectionReviewPrompt,
  buildPRDFullReviewPrompt,
  buildPRDSectionAdjustPrompt,
  PRD_IMPROVE_SYSTEM_PROMPT,
  buildPRDImprovePrompt,
  buildPRDSectionImprovePrompt,
};

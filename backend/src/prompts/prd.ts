/**
 * PRD（产品需求文档）提示词 - 研发&交互可执行增强版
 */

export const PRD_SYSTEM_PROMPT = `
你是一位资深产品经理，长期与前端、后端、测试、交互设计师协作，
擅长输出「研发可直接执行」的产品需求文档（PRD）。

你的目标不是写说明文，而是交付一份可以直接进入研发流程的执行蓝图：
- 研发人员阅读后无需反复追问即可开始开发
- 测试人员可直接基于 PRD 编写测试用例
- 交互设计师可据此还原页面结构与状态

工作原则：
- 所有功能必须明确：触发条件、前置条件、主流程、异常流程、边界条件
- 避免模糊表达（如"尽量""可能""提升体验"等）
- 所有需求必须可验证、可测试、可验收
- 明确需求边界：本期做什么 / 明确不做什么
- 如适用，请区分 MVP 与后续版本规划

输出要求：
- 使用 Markdown
- **必须严格按照提供的 PRD 模板格式输出，保持章节编号和结构完全一致**
- **必须包含模板中的所有章节（0-8章），不得缺失或跳过任何章节**
- 结构清晰、层级合理
- 面向研发与交互团队，而非管理层汇报
`;

export const PRD_TEMPLATE = `# 产品需求文档（PRD）

## 0. 版本说明
- 当前版本：v1.0
- 更新说明：首次版本 / 本次变更点说明
- 更新日期：YYYY-MM-DD

---

## 1. 产品概述

### 1.1 产品名称

### 1.2 产品愿景
- 产品要解决的核心问题
- 长期目标与价值

### 1.3 目标用户
- 用户角色
- 使用频率
- 使用场景

### 1.4 问题陈述
- 当前存在的问题
- 为什么现在要解决

### 1.5 核心使用场景
- 场景 1：背景 + 用户目标
- 场景 2：背景 + 用户目标

---

## 2. 目标与成功指标

### 2.1 业务目标
- 目标描述 + 衡量方式

### 2.2 用户目标
- 用户完成什么即算成功

### 2.3 成功指标（可量化）
- 指标名称：计算口径 / 数据来源 / 目标值

---

## 3. 用户故事

### 3.1 核心用户故事（结构化）

- 用户类型：
- 使用场景：
- 用户目标：
- 操作路径：
- 成功条件：
- 失败 / 异常情况：

### 3.2 次要用户故事

---

## 4. 功能需求

### 4.1 功能清单
- 功能 A（MVP）
- 功能 B（MVP）
- 功能 C（后续）

---

### 4.2 功能详细说明

#### 功能 A：功能名称

**功能描述**
- 一句话说明功能价值

**触发条件**
- 用户点击 / 系统触发 / 定时触发

**前置条件**
- 登录态
- 权限
- 数据状态

**主流程**
1. 用户执行 A
2. 系统校验 B
3. 系统返回 C

**异常流程**
- 异常 1：原因 → 系统反馈
- 异常 2：原因 → 系统反馈

**边界条件**
- 空数据
- 最大 / 最小值
- 高频操作限制

**交互说明**
- Loading 状态
- 禁用态
- 错误提示文案

**验收标准（Given / When / Then）**
- Given：前置条件
- When：用户操作
- Then：系统结果

---

## 5. 页面与交互设计说明

### 5.1 页面列表
- 页面名称 | 路径 | 进入方式

### 5.2 页面结构
- 模块划分
- 组件说明

### 5.3 页面状态设计
- 初始态
- 空态
- 加载态
- 错误态
- 成功态

### 5.4 交互细节

#### 5.4.1 表单交互规范

**输入框交互**
- 聚焦状态：边框高亮、显示提示信息
- 失焦状态：触发校验，显示校验结果
- 实时校验：输入过程中实时反馈（如密码强度）
- 错误提示：错误状态下显示具体错误信息，位置在输入框下方或右侧

**密码输入框校验逻辑示例**
- 长度要求：最少8位，最多20位
- 字符要求：必须包含大小写字母、数字、特殊字符中的至少3种
- 实时校验：输入时实时显示密码强度（弱/中/强）
- 错误提示：
  - 长度不足：显示"密码长度至少8位"
  - 字符类型不足：显示"密码需包含大小写字母、数字、特殊字符中的至少3种"
  - 常见弱密码：提示"该密码过于简单，请使用更复杂的密码"

**邮箱输入框校验逻辑示例**
- 格式校验：必须符合邮箱格式（xxx@xxx.xxx）
- 实时校验：失焦时校验格式
- 错误提示：格式错误时显示"请输入正确的邮箱地址"

**手机号输入框校验逻辑示例**
- 格式校验：11位数字，以1开头
- 实时格式化：输入时自动添加分隔符（如：138-0013-8000）
- 错误提示：格式错误时显示"请输入11位手机号码"

#### 5.4.2 按钮交互规范

**主要按钮（Primary Button）**
- 默认状态：可点击，显示主要颜色
- 悬停状态：颜色加深，鼠标变为手型
- 点击状态：按下时颜色变深，有轻微缩放效果
- 禁用状态：灰色，不可点击，鼠标变为禁止图标
- Loading状态：显示加载动画，按钮文字变为"处理中..."

**次要按钮（Secondary Button）**
- 默认状态：边框样式，背景透明
- 悬停状态：背景色轻微填充
- 点击反馈：与主要按钮相同

#### 5.4.3 列表交互规范

**列表项交互**
- 悬停效果：背景色变化，显示操作按钮
- 选中状态：左侧显示选中标记，背景色高亮
- 点击反馈：点击时有轻微动画反馈
- 加载更多：滚动到底部时自动加载，显示加载动画

#### 5.4.4 弹窗交互规范

**模态弹窗**
- 打开动画：从中心放大，背景遮罩淡入
- 关闭动画：缩小淡出，背景遮罩淡出
- 点击遮罩：点击背景遮罩可关闭弹窗
- ESC键：按ESC键可关闭弹窗
- 焦点管理：打开时焦点移至弹窗内第一个可交互元素

**提示弹窗（Toast）**
- 显示位置：页面顶部或底部居中
- 显示时长：成功提示3秒，错误提示5秒
- 自动关闭：时间到后自动消失
- 手动关闭：提供关闭按钮

#### 5.4.5 搜索交互规范

**搜索框交互**
- 实时搜索：输入后延迟300ms触发搜索（防抖）
- 搜索建议：输入时显示搜索建议列表
- 清空按钮：有内容时显示清空按钮
- 搜索历史：显示最近5条搜索历史

#### 5.4.6 文件上传交互规范

**文件选择**
- 支持格式：明确显示支持的文件格式（如：jpg, png, pdf）
- 文件大小限制：明确显示最大文件大小（如：最大10MB）
- 拖拽上传：支持拖拽文件到上传区域
- 上传进度：显示上传进度条和百分比
- 错误提示：格式错误或大小超限时显示具体错误信息

#### 5.4.7 日期选择交互规范

**日期选择器**
- 默认值：显示当前日期或上次选择日期
- 日期范围：限制可选日期范围（如：不能选择未来日期）
- 快捷选择：提供"今天"、"昨天"、"最近7天"等快捷选项
- 格式显示：选择后以统一格式显示（如：YYYY-MM-DD）

#### 5.4.8 下拉选择交互规范

**下拉选择框**
- 展开动画：下拉列表从上方展开
- 搜索功能：选项较多时支持搜索过滤
- 多选支持：如支持多选，显示已选数量
- 空状态：无选项时显示"暂无数据"

#### 5.4.9 分页交互规范

**分页组件**
- 页码显示：显示当前页、总页数、总条数
- 跳转功能：支持输入页码跳转
- 每页条数：支持选择每页显示条数（10/20/50/100）
- 禁用状态：首页/末页时禁用对应按钮

#### 5.4.10 动效说明

**页面切换动效**
- 页面进入：从右侧滑入
- 页面退出：向左侧滑出
- 动画时长：300ms

**加载动效**
- 骨架屏：数据加载时显示骨架屏
- 加载动画：使用统一的加载动画样式
- 加载提示：显示"加载中..."文字提示

---

## 6. 非功能需求

### 6.1 性能
- 接口响应时间
- 并发要求

### 6.2 安全性
- 鉴权方式
- 数据安全要求

### 6.3 可用性
- 可访问性
- 错误可理解性

### 6.4 可扩展性
- 模块化要求
- 配置化能力

---

## 7. 验收与交付标准（Definition of Done）

- 功能完整性
- 交互一致性
- 异常覆盖
- 性能达标

---

## 8. 风险与应对

### 8.1 技术风险
- 风险描述 + 应对策略

### 8.2 业务风险
- 风险描述 + 应对策略
`;

export function buildPRDPrompt(input: string): string {
  return `基于以下市场研究文档（MRD），生成一份【研发与交互可直接执行】的产品需求文档（PRD）：

【市场研究文档（MRD）】
${input}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

生成要求：
1. **必须严格按照上述 PRD 模板结构输出，不得缺失任何章节**
2. **必须保持与模板完全一致的章节编号和层级结构**（## 0. 版本说明、## 1. 产品概述、## 2. 目标与成功指标...一直到 ## 8. 风险与应对）
3. 所有功能必须包含：触发条件、前置条件、主流程、异常流程、边界条件、验收标准
4. 明确需求边界：本期做什么 / 不做什么
5. 补充页面结构、状态设计与详细的交互细节（包括表单校验逻辑、按钮交互、列表交互等）
6. 交互细节必须具体可执行，例如：密码输入框的校验规则、错误提示文案、实时校验逻辑等
7. 避免模糊表述，所有内容需可测试、可验收

硬性要求：
- 输出完整 Markdown PRD，必须包含模板中的所有章节（0-8章）
- 内容不少于 3000 字
- 不保留任何占位符（如"功能 A"、"场景 1"等占位文本需替换为实际内容）
- 章节编号和标题必须与模板完全一致
- 第5章"页面与交互设计说明"必须包含详细的交互细节，特别是表单校验逻辑
`;
}

export function buildPRDUpdatePrompt(historyPRD: string, newRequirements: string): string {
  return `基于以下历史 PRD 与新增需求，生成更新后的完整 PRD 文档：

【历史 PRD】
${historyPRD}

【新增 / 变更需求】
${newRequirements}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

更新规则：
1. **必须严格按照 PRD 模板结构输出，保持所有章节（0-8章）完整**
2. **必须保持与模板完全一致的章节编号和层级结构**
3. 未被新需求影响的内容需完整保留
4. 变更内容需明确标注（新增 / 修改）
5. 在文档顶部（## 0. 版本说明）补充版本更新说明

输出要求：
- 完整 Markdown PRD，必须包含模板中的所有章节（0-8章）
- 每个功能仍需满足研发级描述标准
- 内容不少于 3000 字
- 章节编号和标题必须与模板完全一致
- 交互细节必须详细具体，包含表单校验逻辑等
`;
}

export function buildPRDWithRAGPrompt(
  query: string,
  relevantChunks: string,
  newRequirements: string
): string {
  return `请基于以下历史 PRD 片段与新的市场研究文档（MRD），生成或更新完整 PRD：

【检索到的历史PRD内容】
${relevantChunks}

【用户关注点】
${query}

【新的市场研究文档（MRD）】
${newRequirements}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

生成要求：
1. **必须严格按照 PRD 模板结构输出，保持所有章节（0-8章）完整**
2. **必须保持与模板完全一致的章节编号和层级结构**
3. 保持与历史 PRD 的一致性
4. 如存在冲突，以新需求为准并说明原因
5. 所有功能需满足研发与测试可直接使用标准

输出要求：
- 完整 Markdown PRD，必须包含模板中的所有章节（0-8章）
- 内容不少于 3000 字
- 章节编号和标题必须与模板完全一致
- 交互细节必须详细具体
`;
}

/**
 * 生成 PRD 目录（仅输出章节结构）
 */
export function buildPRDOutlinePrompt(input: string): string {
  return `基于以下市场研究文档（MRD），生成 PRD 的目录结构（仅输出章节标题，不输出内容）：

【市场研究文档（MRD）】
${input}

【PRD 模板格式（必须严格遵循）】
${PRD_TEMPLATE}

要求：
1. **严格按照模板结构输出目录，包含所有一级章节（## 0. 到 ## 8.）**
2. **只输出 Markdown 格式的目录，格式如下：**
\`\`\`
## 0. 版本说明
## 1. 产品概述
## 2. 目标与成功指标
...
## 8. 风险与应对
\`\`\`
3. 不要输出任何章节内容，只输出章节标题
4. 保持章节编号和标题与模板完全一致

输出格式：纯 Markdown 目录，每个章节一行
`;
}

/**
 * 生成单个章节的详细内容
 */
export function buildPRDSectionPrompt(
  input: string,
  outline: string,
  sectionNumber: number,
  sectionTitle: string
): string {
  return `基于以下市场研究文档（MRD）和 PRD 目录，生成第 ${sectionNumber} 章「${sectionTitle}」的详细内容：

【市场研究文档（MRD）】
${input}

【PRD 目录】
${outline}

【目标章节】
## ${sectionNumber}. ${sectionTitle}

生成要求：
1. **严格按照 PRD 模板中第 ${sectionNumber} 章的结构和格式输出**
2. **必须包含该章节下的所有子章节和内容点**
3. 内容要详细、具体、可执行，避免空洞和占位符
4. 所有功能描述必须包含：触发条件、前置条件、主流程、异常流程、边界条件、验收标准
5. 内容要面向研发和测试团队，确保可直接使用

输出要求：
- 只输出第 ${sectionNumber} 章的内容（包含章节标题）
- 内容要充实，不少于 500 字
- 使用 Markdown 格式
- 不保留任何占位符
`;
}

/**
 * PRD Review 提示词
 */
export const PRD_REVIEW_SYSTEM_PROMPT = `你是一位资深的产品文档审查专家，擅长检查 PRD 文档的完整性和质量。

你的职责是：
- 检查 PRD 是否包含所有必需的章节（0-8章）
- 检查每个章节的内容是否充实、具体
- 识别空洞、模糊或占位符内容
- 特别检查交互细节是否详细具体（如表单校验逻辑）
- 提供改进建议

输出格式：结构化的审查报告`;

export function buildPRDReviewPrompt(prdContent: string, outline: string): string {
  return `请审查以下 PRD 文档的质量：

【PRD 文档】
${prdContent}

【预期目录结构】
${outline}

审查要求：
1. **检查章节完整性**：是否包含所有必需的章节（## 0. 版本说明 到 ## 8. 风险与应对）？
2. **检查内容质量**：每个章节是否有充实的内容？是否存在空洞、模糊或占位符？
3. **检查格式规范**：章节编号、标题是否与模板一致？
4. **检查可执行性**：功能描述是否包含触发条件、主流程、异常流程、边界条件、验收标准？
5. **检查交互细节**：第5章是否包含详细的交互细节？表单校验逻辑是否具体？是否包含错误提示文案？

输出格式：
\`\`\`markdown
# PRD 审查报告

## 1. 章节完整性检查
- [ ] 章节 0. 版本说明：存在/缺失
- [ ] 章节 1. 产品概述：存在/缺失
- [ ] 章节 2. 目标与成功指标：存在/缺失
- [ ] 章节 3. 用户故事：存在/缺失
- [ ] 章节 4. 功能需求：存在/缺失
- [ ] 章节 5. 页面与交互设计说明：存在/缺失（需特别检查交互细节是否详细）
- [ ] 章节 6. 非功能需求：存在/缺失
- [ ] 章节 7. 验收与交付标准：存在/缺失
- [ ] 章节 8. 风险与应对：存在/缺失

## 2. 内容质量检查
### 发现的问题：
1. [章节编号] 问题描述
2. [章节编号] 问题描述

### 空洞内容识别：
- [章节编号] 具体位置和问题

### 交互细节检查：
- [ ] 第5章是否包含详细的表单校验逻辑（如密码输入框的校验规则）？
- [ ] 是否包含具体的错误提示文案？
- [ ] 是否包含按钮、列表、弹窗等组件的交互规范？
- [ ] 交互细节是否足够具体，研发可直接实现？

## 3. 改进建议
1. 建议 1
2. 建议 2

## 4. 审查结论
- 通过 / 需要改进
- 主要问题：[列出主要问题]
\`\`\`
`;
}

/**
 * 审核单个章节的提示词
 */
export function buildPRDSectionReviewPrompt(
  sectionContent: string,
  sectionNumber: number,
  sectionTitle: string,
  outline: string
): string {
  return `请审查以下 PRD 章节的质量：

【章节内容】
## ${sectionNumber}. ${sectionTitle}

${sectionContent}

【完整目录结构】
${outline}

审查要求：
1. **检查内容充实度**：章节内容是否充实、具体？是否存在空洞、模糊或占位符？
2. **检查格式规范**：章节编号、标题是否与目录一致？
3. **检查可执行性**：如果是功能需求章节，是否包含触发条件、主流程、异常流程、边界条件、验收标准？
4. **检查交互细节**：如果是交互设计章节，是否包含详细的交互细节？表单校验逻辑是否具体？是否包含错误提示文案？
5. **检查完整性**：章节是否包含该章节应该包含的所有子章节和内容？

输出格式：
\`\`\`markdown
## 章节 ${sectionNumber}. ${sectionTitle} 审查结果

### 1. 内容质量
- [ ] 内容充实：是/否
- [ ] 内容具体：是/否
- [ ] 无占位符：是/否

### 2. 发现的问题
1. 问题描述（具体位置）
2. 问题描述（具体位置）

### 3. 改进建议
1. 建议 1
2. 建议 2

### 4. 审查结论
- 通过 / 需要改进
- 主要问题：[列出主要问题]
\`\`\`
`;
}

/**
 * 调整单个章节的提示词
 */
export function buildPRDSectionAdjustPrompt(
  originalSectionContent: string,
  sectionNumber: number,
  sectionTitle: string,
  userRequest: string,
  fullPRDContext?: string,
  conversationHistory?: string
): string {
  const contextPart = fullPRDContext
    ? `\n\n【完整 PRD 上下文（仅供参考）】\n${fullPRDContext}`
    : '';
  
  const historyPart = conversationHistory || '';

  return `请根据用户的要求，调整以下 PRD 章节的内容：

【原始章节内容】
## ${sectionNumber}. ${sectionTitle}

${originalSectionContent}
${historyPart}

【用户调整要求】
${userRequest}
${contextPart}

调整要求：
1. **保持章节标题格式不变**：## ${sectionNumber}. ${sectionTitle}
2. **根据用户要求调整章节内容**，确保调整后的内容：
   - 符合 PRD 模板中该章节的结构要求
   - 保持与整体 PRD 的一致性
   - 内容详细、具体、可执行
   - 面向研发和测试团队
   - ${historyPart ? '**考虑之前的对话历史，保持调整的连贯性**' : ''}
3. **只输出调整后的章节内容**（包含章节标题）
4. **不要输出其他章节的内容**
5. 如果用户要求与 PRD 模板结构冲突，优先满足用户要求，但保持格式规范

输出格式：Markdown 格式的章节内容
`;
}

/**
 * 根据审查报告改进PRD文档的提示词
 */
export const PRD_IMPROVE_SYSTEM_PROMPT = `你是一位资深的产品文档改进专家，擅长根据审查报告的建议，补充和完善PRD文档。

你的职责是：
- 仔细分析审查报告中的改进建议
- 识别文档中需要补充和完善的部分
- 针对性地改进文档内容，使其更加详细、具体、可执行
- 保持文档的原有结构和格式
- 确保改进后的内容符合PRD模板要求

改进原则：
- 保持文档的章节结构和编号不变
- 根据审查报告中的具体建议，补充缺失的内容
- 完善模糊或简略的描述，使其更加详细具体
- 确保所有功能描述包含：触发条件、前置条件、主流程、异常流程、边界条件、验收标准
- 改进后的内容要面向研发和测试团队，确保可直接使用`;

export function buildPRDImprovePrompt(
  prdContent: string,
  reviewReport: string
): string {
  return `请根据以下审查报告的建议，改进和完善PRD文档：

【当前PRD文档】
${prdContent}

【审查报告】
${reviewReport}

改进要求：
1. **仔细分析审查报告**：识别所有改进建议和问题点
2. **保持文档结构**：不要改变章节编号和标题，只改进内容
3. **针对性改进**：
   - 补充缺失的章节内容
   - 完善简略或模糊的描述
   - 补充功能需求的触发条件、异常流程等
   - 细化交互细节和错误提示文案
   - 补充版本说明等元数据
4. **内容质量**：
   - 所有内容必须详细、具体、可执行
   - 避免空洞、模糊或占位符内容
   - 确保研发和测试团队可直接使用
5. **保持格式**：使用Markdown格式，保持章节层级清晰

输出要求：
- 输出完整的改进后的PRD文档
- 保持所有章节（## 0. 到 ## 8.）
- 确保改进后的内容解决了审查报告中提出的所有问题
- 如果审查报告建议增加新章节，可以在现有章节基础上扩展，但保持主要结构不变
`;
}

export default {
  PRD_SYSTEM_PROMPT,
  PRD_TEMPLATE,
  buildPRDPrompt,
  buildPRDUpdatePrompt,
  buildPRDWithRAGPrompt,
  buildPRDOutlinePrompt,
  buildPRDSectionPrompt,
  PRD_REVIEW_SYSTEM_PROMPT,
  buildPRDReviewPrompt,
  buildPRDSectionReviewPrompt,
  buildPRDSectionAdjustPrompt,
  PRD_IMPROVE_SYSTEM_PROMPT,
  buildPRDImprovePrompt,
};
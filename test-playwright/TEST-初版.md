# 测试文档

---

## 第二部分：测试用例

### 用例编写规范

#### 优先级定义

| 优先级 | 定义     | 说明                       |
| ------ | -------- | -------------------------- |
| P0     | 核心功能 | 主流程、核心业务，必须通过 |
| P1     | 重要功能 | 重要分支、常用功能         |
| P2     | 一般功能 | 边界条件、异常处理         |
| P3     | 低优先级 | 极端场景、优化建议         |

#### 用例格式说明

- 采用 **Given-When-Then** 格式（BDD风格）
- **Given**：前置条件和测试数据准备
- **When**：执行的操作步骤（含具体导航路径，可直接映射为 Playwright action）
- **Then**：预期结果验证（仅页面可观测结果，可直接映射为 Playwright assertion）

---

### 模块1：渠道配置入口

#### TC-001：渠道配置入口 - 点击按钮打开渠道配置弹窗

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-001                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 用户已登录管理后台且具备渠道订单管理权限 |
| 关联需求 | PRD-功能1                             |

**Given**：

- 用户角色为运营人员或管理员
- 用户已进入渠道订单管理页

**When**：

- 在 extra-buttons 区域点击「渠道配置」按钮
- 等待弹窗或抽屉加载完成

**Then**：

- 2 秒内打开渠道配置弹窗或抽屉
- 弹窗或抽屉内展示渠道列表区域

---

#### TC-002：渠道配置入口 - 加载失败时提示

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-002                        |
| 类型     | 管理后台                              |
| 优先级   | P2                                    |
| 前置条件 | 用户已登录管理后台，渠道列表接口异常或不可用 |
| 关联需求 | PRD-功能1                             |

**Given**：

- 用户已进入渠道订单管理页
- 渠道列表接口返回失败或超时

**When**：

- 在 extra-buttons 区域点击「渠道配置」按钮
- 等待系统响应

**Then**：

- 出现「加载失败，请重试」提示
- 弹窗或抽屉内展示错误提示区域

---

### 模块2：渠道配置展示与新增

#### TC-003：渠道配置展示 - 查看已配置渠道列表

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-003                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 系统中已存在至少 1 个已配置渠道       |
| 关联需求 | PRD-功能2                             |

**Given**：

- 用户已打开渠道配置弹窗或抽屉
- 系统中已有历史配置渠道

**When**：

- 等待渠道列表加载完成

**Then**：

- 渠道列表中展示历史配置渠道与新增渠道
- 列表中无删除、编辑入口

---

#### TC-004：渠道配置新增 - 新增渠道成功

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-004                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 用户已打开渠道配置弹窗，新渠道编码在系统中不存在 |
| 关联需求 | PRD-功能2                             |

**Given**：

- 用户已打开渠道配置弹窗或抽屉
- 准备新增渠道名称「测试渠道A」、编码「test_channel_a」（编码未被占用）

**When**：

- 点击「新增」或「新增渠道」按钮
- 填写名称「测试渠道A」、编码「test_channel_a」
- 点击提交或保存按钮
- 等待系统保存完成

**Then**：

- 出现保存成功或新增成功提示
- 渠道列表刷新，新渠道「测试渠道A」出现在列表中

---

#### TC-005：渠道配置新增 - 编码重复时提示错误

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-005                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 系统中已存在编码为「wechat」的渠道     |
| 关联需求 | PRD-功能2                             |

**Given**：

- 用户已打开渠道配置弹窗或抽屉
- 系统中已存在编码「wechat」的渠道

**When**：

- 点击「新增」或「新增渠道」按钮
- 填写名称「新微信渠道」、编码「wechat」（与已有渠道重复）
- 点击提交或保存按钮

**Then**：

- 出现「该编码已存在，请更换」或类似错误提示
- 表单未提交，渠道列表未新增该渠道

---

#### TC-006：渠道配置新增 - 必填项未填时提示

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-006                        |
| 类型     | 管理后台                              |
| 优先级   | P2                                    |
| 前置条件 | 用户已打开渠道配置弹窗                 |
| 关联需求 | PRD-功能2                             |

**Given**：

- 用户已打开渠道配置弹窗或抽屉
- 新增表单中名称为空、编码为空或其一为空

**When**：

- 点击「新增」或「新增渠道」按钮
- 不填写名称或编码（或仅填写其一）
- 点击提交或保存按钮

**Then**：

- 出现必填项未填的提示，提示具体字段（如「请填写名称」或「请填写编码」）
- 表单未提交

---

### 模块3：CSV 导入与渠道联动

#### TC-007：CSV 导入渠道联动 - 购买渠道下拉框展示已配置渠道

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-007                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 系统中已存在至少 1 个已配置渠道       |
| 关联需求 | PRD-功能3                             |

**Given**：

- 用户已进入渠道订单管理页
- 系统中已配置渠道（如微信、抖音等）

**When**：

- 点击「导入渠道订单」按钮
- 等待导入弹窗打开
- 点击「购买渠道」下拉框

**Then**：

- 购买渠道下拉框展示所有已配置渠道
- 下拉选项与渠道配置列表一致

---

#### TC-008：CSV 导入渠道联动 - 新增渠道后导入时可选中

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-008                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 用户刚在渠道配置中新增渠道「新渠道B」  |
| 关联需求 | PRD-功能3                             |

**Given**：

- 用户已在渠道配置中新增渠道「新渠道B」并关闭弹窗
- 导入弹窗当前未打开或已关闭

**When**：

- 点击「导入渠道订单」按钮
- 等待导入弹窗打开
- 点击「购买渠道」下拉框

**Then**：

- 购买渠道下拉框中出现「新渠道B」选项
- 可选择「新渠道B」

---

#### TC-009：CSV 导入渠道联动 - 选择新渠道上传 CSV 成功导入

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-009                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 已配置渠道「新渠道C」，且准备有效 CSV 文件 |
| 关联需求 | PRD-功能3                             |

**Given**：

- 用户已打开导入渠道订单弹窗
- 购买渠道下拉框中存在「新渠道C」
- 用户已准备符合格式的渠道订单 CSV 文件

**When**：

- 在购买渠道下拉框中选择「新渠道C」
- 上传 CSV 文件
- 点击提交或导入按钮
- 等待导入完成

**Then**：

- 出现导入成功提示
- 导入弹窗关闭或显示导入完成状态

---

#### TC-010：CSV 导入渠道联动 - 渠道列表加载失败时提示

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-010                        |
| 类型     | 管理后台                              |
| 优先级   | P2                                    |
| 前置条件 | 渠道列表接口异常或不可用              |
| 关联需求 | PRD-功能3                             |

**Given**：

- 用户已进入渠道订单管理页
- 渠道列表接口返回失败或超时

**When**：

- 点击「导入渠道订单」按钮
- 等待导入弹窗打开并加载渠道列表

**Then**：

- 出现「加载渠道失败，请重试」或类似提示

---

### 模块4：CSV 映射配置与渠道联动

#### TC-011：CSV 映射配置渠道联动 - Tab 按已配置渠道展示

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-011                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 系统中已存在至少 1 个已配置渠道       |
| 关联需求 | PRD-功能4                             |

**Given**：

- 用户已进入渠道订单管理页
- 系统中已配置渠道（如微信、抖音等）

**When**：

- 点击「CSV 映射配置」按钮
- 等待映射配置弹窗打开

**Then**：

- 映射配置弹窗中 Tab 按已配置渠道展示
- Tab 与渠道配置列表一致

---

#### TC-012：CSV 映射配置渠道联动 - 新增渠道后出现对应 Tab

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-012                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 用户刚在渠道配置中新增渠道「新渠道D」  |
| 关联需求 | PRD-功能4                             |

**Given**：

- 用户已在渠道配置中新增渠道「新渠道D」并关闭弹窗
- 映射配置弹窗当前未打开或已关闭

**When**：

- 点击「CSV 映射配置」按钮
- 等待映射配置弹窗打开

**Then**：

- 映射配置弹窗中出现「新渠道D」对应的 Tab
- 可点击该 Tab 切换

---

#### TC-013：CSV 映射配置渠道联动 - 新渠道 Tab 配置字段映射并保存

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-013                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 已配置渠道「新渠道E」，映射配置弹窗已打开 |
| 关联需求 | PRD-功能4                             |

**Given**：

- 用户已打开 CSV 映射配置弹窗
- 存在「新渠道E」对应的 Tab

**When**：

- 点击「新渠道E」Tab
- 配置字段映射（如订单号、金额等字段对应关系）
- 点击保存按钮
- 等待保存完成

**Then**：

- 出现保存成功提示
- 映射配置弹窗内该 Tab 的配置已更新

---

#### TC-014：CSV 映射配置渠道联动 - 渠道列表加载失败时提示

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-014                        |
| 类型     | 管理后台                              |
| 优先级   | P2                                    |
| 前置条件 | 渠道列表接口异常或不可用              |
| 关联需求 | PRD-功能4                             |

**Given**：

- 用户已进入渠道订单管理页
- 渠道列表接口返回失败或超时

**When**：

- 点击「CSV 映射配置」按钮
- 等待映射配置弹窗打开并加载渠道列表

**Then**：

- 出现「加载渠道失败，请重试」或类似提示

---

### 模块5：端到端流程

#### TC-015：端到端 - 新增渠道并完成首次导入全流程

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| 用例ID   | TC-channel-015                        |
| 类型     | 管理后台                              |
| 优先级   | P0                                    |
| 前置条件 | 用户已登录管理后台，准备新渠道名称与编码及有效 CSV 文件 |
| 关联需求 | PRD-场景1                             |

**Given**：

- 用户角色为运营人员或管理员
- 用户已进入渠道订单管理页
- 准备新增渠道名称「全流程测试渠道」、编码「e2e_test」
- 准备符合格式的渠道订单 CSV 文件

**When**：

- 点击「渠道配置」按钮 → 等待弹窗打开
- 点击「新增渠道」→ 填写名称「全流程测试渠道」、编码「e2e_test」→ 提交 → 出现成功提示后关闭弹窗
- 点击「导入渠道订单」→ 等待弹窗打开
- 在购买渠道下拉框中选择「全流程测试渠道」
- 上传 CSV 文件 → 点击导入 → 等待导入完成

**Then**：

- 渠道配置中新增成功，列表中出现「全流程测试渠道」
- 导入弹窗中购买渠道下拉框可选中「全流程测试渠道」
- 导入完成后出现导入成功提示

---

## 附录：测试用例生成指南

### 强制要求

1. **用例覆盖完整性**：
   - 必须覆盖PRD中所有功能点
   - 必须包含正向场景和异常场景
   - 必须包含边界条件测试

2. **用例格式规范**：
   - 统一使用 Given-When-Then 格式
   - 每个用例必须有唯一ID
   - 每个用例必须标注优先级
   - 每个用例必须标注类型（适用端）

3. **测试数据要求**：
   - 测试数据必须具体、可执行
   - 不允许使用模糊描述如"某用户"
   - 必须包含边界值数据

4. **预期结果要求（Then）**：
   - Then 只写**用户在页面上可观测到的结果**（提示信息、列表变化、按钮状态等）
   - Then 断言的目标元素必须存在于 When 最后一步所在的页面上（When-Then 联动）
   - 每条 Then 需能映射为 Playwright 断言：元素可见、文本包含、元素状态变化
   - 禁止写数据库断言（如 `数据库中 xxx 字段已更新`）
   - 禁止写硬编码时间（如 `2秒内`），改为 `出现xxx提示`
   - 禁止写硬编码数量（如 `已成功设置 3 台`），改为 `出现成功数量反馈`
   - 禁止写接口级断言（如 `接口返回数据包含 xxx`）
   - 禁止写主观/不可验证描述（如 `便于快速找到xxx`）
   - 数据前提（设备数量、初始状态等）放在 Given 中，不放在 Then 中
   - 异常场景必须验证用户可见的错误提示

5. **操作步骤要求（When）**：
   - UI 用例的 When 必须包含具体的导航路径（菜单点击顺序），不使用抽象描述
   - 每步点击后标注等待点（如 → 等待表格加载、→ 等待子菜单展开）
   - 导航步骤用 → 连接，先写导航再写业务操作

6. **自动化可行性标注**：
   - 对于非 UI 可验证的用例（后台任务、cron job、数据库直接操作），在属性表 `类型` 列标注为 `通用（不可UI自动化）`

7. **用例数量要求**：
   - 每个功能模块至少3个测试用例
   - 包含：正向场景、边界条件、异常场景

### 输出格式要求

- 使用 Markdown 格式
- 严格按照测试文档模板结构
- 用例描述清晰、步骤可执行

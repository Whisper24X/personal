# Generated Expo Template

This template is used by AutoFlow when `targetPlatform` includes `app`.

## Scripts

- `npm run test:app:jest`: placeholder Jest/mobile unit runner.
- `npm run test:app:detox`: placeholder Detox runner. Fails when Detox is unavailable.
- `npm run test:app`: runs Jest then Detox.

## Notes

- Replace placeholders with real Expo/Jest/Detox setup in your project.
- Keep script names stable so AutoFlow step 5 can execute app tests.

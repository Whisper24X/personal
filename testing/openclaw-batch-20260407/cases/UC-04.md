# [P0] UC-04 刷新会话

## 前置条件

前置条件

1. 用户已完成登录，且本地持有 refresh token（以实际前端实现为准）。

## 执行步骤

执行步骤

1. 在已登录状态下，触发刷新访问令牌流程（如前端静默刷新、或手动调用刷新接口）。

## 预期结果

预期结果

1. 调用后端接口（Bearer 为 refresh token）返回 HTTP 200，响应中含新的 access token（及可选新的 refresh token）。
2. 业务请求可使用新 access token 继续访问。

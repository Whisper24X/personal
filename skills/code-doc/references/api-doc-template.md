# API 接口变更清单 - {版本号}

> **生成时间**：{当前实际执行时间，精确到秒，格式 YYYY-MM-DD HH:mm:ss}
> **新增接口**：{N} 个 | **修改接口**：{N} 个 | **删除接口**：{N} 个

---

## 新增接口

### {模块名}

#### {接口名称}

| 项目         | 内容                             |
| ------------ | -------------------------------- |
| **接口地址** | `{METHOD} {/path}`               |
| **接口描述** | {接口用途说明}                   |
| **认证方式** | {Bearer Token / API Key / 无 等} |

**请求参数**

| 参数名  | 位置              | 类型   | 必填  | 说明                       |
| ------- | ----------------- | ------ | ----- | -------------------------- |
| {param} | {Body/Query/Path} | {type} | 是/否 | {说明，含取值范围或枚举值} |

**响应参数**

| 参数名  | 类型   | 说明   |
| ------- | ------ | ------ |
| {field} | {type} | {说明} |

**响应示例**

```json
{}
```

---

## 修改接口

### {模块名}

#### {接口名称}

| 项目         | 内容                                                |
| ------------ | --------------------------------------------------- |
| **接口地址** | `{METHOD} {/path}`                                  |
| **接口描述** | {接口用途说明}                                      |
| **认证方式** | {Bearer Token / API Key / 无 等}                    |
| **变更说明** | {本次修改了什么：新增字段 / 删除字段 / 逻辑变更 等} |

**请求参数**

| 参数名  | 位置              | 类型   | 必填  | 说明                               |
| ------- | ----------------- | ------ | ----- | ---------------------------------- |
| {param} | {Body/Query/Path} | {type} | 是/否 | {说明，★ 标注本次新增或变更的参数} |

**响应参数**

| 参数名  | 类型   | 说明                               |
| ------- | ------ | ---------------------------------- |
| {field} | {type} | {说明，★ 标注本次新增或变更的字段} |

**响应示例**

```json
{}
```

---

## 删除接口

| 接口地址           | 模块     | 说明                 |
| ------------------ | -------- | -------------------- |
| `{METHOD} {/path}` | {模块名} | {删除原因或替代接口} |

---

## 统计汇总

| 维度       | 数量 |
| ---------- | ---- |
| 新增接口   | -    |
| 修改接口   | -    |
| 删除接口   | -    |
| 涉及模块数 | -    |

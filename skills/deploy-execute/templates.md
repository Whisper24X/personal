# deploy-execute 输出模板

## deployLog.md 模板

```markdown
# 部署执行日志

部署时间: [当前时间]
执行命令: [实际执行的命令]
执行耗时: [总耗时]

## 执行过程

### 停止现有服务

[停止命令的输出]

### 启动服务

[启动命令的输出]

## 服务启动状态

| 服务名称 | 状态   | 访问地址 | 启动耗时 |
| -------- | ------ | -------- | -------- |
| [服务1]  | [状态] | [地址]   | [耗时]   |

## 错误记录（如有）

[错误日志内容]
```

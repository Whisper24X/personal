# 复制为 .env 后填写；勿将 .env 提交到 Git
LOGIN_URL=https://trip-shadow-test.yangcong345.com/trip/login
LOGIN_USER=19371968034
LOGIN_PASS=12345678Dyw

# 服务状态映射行：系统侧下拉选项（用例未写死时与 runbook 一致，默认「待预约」）
TC013_SERVICE_STATUS=待预约

# 可选：除登录页外的站点 origin，便于相对路径跳转（本用例主要使用 LOGIN_URL）
# BASE_URL=https://example.com

# 无头模式（不设则本地默认「有头」显示浏览器；CI 会自动无头）
# HEADLESS=1

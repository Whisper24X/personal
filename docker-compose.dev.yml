services:
  backend:
    image: ainative/backend:dev
    build:
      context: ./backend
      dockerfile: Dockerfile

  frontend:
    image: ainative/frontend:dev
    build:
      context: ./frontend
      dockerfile: Dockerfile
